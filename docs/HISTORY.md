# Saaksh — shipped-work history

Everything here is **already shipped and live** unless a line says otherwise. It was moved out of
`CLAUDE.md` on 2026-09-09 because that file is auto-loaded into every Claude Code session and had
grown to 161k characters (~40k tokens before a word is typed).

Read this when you need the *why* behind an existing feature, or want to know whether something was
already tried. For the current state — deploy status, open priorities, architecture, the design
system — stay in `CLAUDE.md`. For the reasoning behind product decisions, `docs/DECISIONS.md`.
Newest first.

## Project Status — earlier: last updated 2026-09-04

**Everything below through 2026-08-01 is pushed and deployed.** The old "12 commits unpushed"
warning is gone: that sprint went out, and origin/master == local == live. Deploys are still
`$env:NODE_TLS_REJECT_UNAUTHORIZED="0"; vercel --prod --yes` and still ship the WORKING TREE,
so run `git status` first.

### What the numbers actually say (GA4, read 2026-09-04)

Property `a396601351p539979083` under **rahulu626@gmail.com** (not the account Chrome defaults
to). **Analytics is opt-IN**, so every count below is a FLOOR, not a total; ratios are sound,
absolutes are not.

- **May 1 to Sep 4: 175 users, 434 sessions, average engagement 6m 31s.** That engagement number
  is genuinely strong. The people who arrive use the thing properly.
- **Traffic is the constraint, not the product.** 175 users in four months, and 175 of them new:
  essentially nobody returns.
- **AI assistants are now the largest channel.** Over 28 days: AI Assistant 25 sessions (49%),
  Organic Search 18 (35%), Direct 7, Organic Social 1. Over four months Direct still leads
  (236) with LinkedIn second (92), so ChatGPT is the fastest-GROWING channel, not the biggest
  historically. It engages worst though (28s vs organic search's 1m 24s): people arrive with a
  specific question and leave without an answer. That is an addressable product problem.
- **Best pages:** /report (4m 06s, 59 users over four months) and /jobs (7 active users in 28
  days, the single most-visited page by users). /brief has 3 users and 0 new: the PWA, swipe
  feed and web-push build has essentially no audience. Do not extend it.
- **The funnel leaks at the LEAD forms, not the intake form.** 41 users hit `form_start` and 59
  reached a report, so the intake works. It is the email capture and Pro request that convert
  to almost nothing: 2 subscribers and 1 access request in the database, and 5 file downloads
  in four months.
- GA4 had ZERO custom events until 2026-09-04, which is why every report read "Key events:
  0.00". Fixed (see below). **USER TO-DO: once events have appeared for a day or two, mark
  report_generated, pro_access_requested, newsletter_subscribed and gig_submitted as key events
  in GA4 Admin.**

### What the consultant WhatsApp group says (five weeks read, to 2026-09-03)

Load-bearing, because it is the ICP talking unprompted:

1. **The work that circulates there is FREELANCE GIGS, not jobs** - an LCA study in Chennai, an
   ISO 13485 lead auditor, VVB empanelment, an ash-dyke audit in Aligarh, an EIA coordinator,
   an LCA/PCF study for a petrochemical firm. Of 107 scraped listings on our board, 93 are
   full-time and 4 are contract (the same role stored four times). iimjobs and Indeed do not
   carry this work at all, so it can only be submitted, never crawled.
2. **"Who can do this?" is the most repeated question, and the only answer is ever "DM me."**
   That is a search with no index, and it is why /directory exists.
3. **Pricing is asked and never answered.** "How much for a CBAM readiness assessment?" and "am
   I underpricing my ESG/LCA/GIS freelance work?" both got only "it depends". NOT YET BUILT and
   the strongest remaining content idea: it is exactly what gets asked of ChatGPT, our
   fastest-growing channel.
4. **People hand over an email instantly for a SPECIFIC artifact.** One offer of training
   material drew nine emails in nine minutes. Saaksh has two subscribers in four months. The
   ask is the problem: "subscribe for updates" versus a named, downloadable thing.
5. Someone said outright that answers should go in the group rather than DMs "it will help our
   whole community", and two people agreed. Unanswered there: GRI 1 Requirement 9 notification,
   whether CSR 2% money may fund carbon-credit projects, ISO 14001/45001 document registers.
   Cited answers are the whole positioning; this is a content seam.
6. Webinars work in this community (an EcoVadis session drew 100 registrations off a Google
   Form). Untried by us.

**Do NOT seed the boards from that group.** Those posts carry people's names and phone numbers
and were shared in a private space. Ask the poster.

### Shipped 2026-09-04

- **/notrack** - open the link once on a device and it is excluded from analytics (GA4,
  Mixpanel and Vercel all sit behind one consent gate, so nothing loads). `?notrack=1` works on
  any URL. Applied inside getConsent so it lands before AnalyticsGate mounts anything.
- **Jobs board fixes.** Dedup was keyed on the raw apply URL while iimjobs appends the listing
  POSITION (`?jobPos=14`), so one role was stored nine times and 67 of 107 rows were duplicates;
  `canonicalUrl` fixed it and the board went 125 cards to 58. Two of seven scraper sources
  rendered an empty shell (`/k/brsr-jobs`, `/k/climate-change-jobs`) which is why BRSR-reporting
  was empty; replaced with `/k/ehs-jobs` and a second Indeed query. Category prompt now has real
  definitions. Employer placeholders ("Unknown", "(Unnamed)") no longer show as real companies.
- **Sort control** (newest / oldest / company A-Z; deliberately no salary sort, it is prose).
- **Motion pass** from the review-animations skill: both mobile sheets were using a DROPDOWN
  animation while anchored to the bottom of the screen; hover is now gated behind a real pointer.
- **Sticky columns** were parked 51px behind the sticky header, clipping the job title. New
  `--site-header-h` token.
- **GA4 event bridge**: `track()` now mirrors every event into gtag, covering all ~30 call sites.
- **Freelance gigs** (`/jobs` toggle) and the **consultant directory** (`/directory`). Both are
  curated from submissions into `src/data/gigs.json` / `src/data/consultants.json`; the actions
  reuse the access-request table with `[GIG]` / `[DIRECTORY]` markers so neither needs a
  migration. A directory profile never shows an email or phone, only the link the consultant
  gives. **Both are EMPTY and will not fill themselves.**
- **Jobs promoted to top-level nav** - it was the best-performing page and was hidden in a dropdown.

### Open, in the order I would do them

1. **Seed the two boards.** 3-4 gigs, 5-6 consultants, asked for by hand. Everything else waits
   on this; an empty board teaches nobody anything.
2. **Mark the GA4 key events** (a day or two after 2026-09-04).
3. **Pricing benchmark page.** Asked twice in five weeks, never answered, and ChatGPT-shaped.
4. **Change the email ask** to a named artifact rather than "updates".
5. **Send the ECS approach.** `docs/ECS-proposal.html` is written and committed; the WhatsApp
   intro to Mayuri Ganguly has not been sent. GTM is the actual bottleneck, not features.
6. Optional, unlocks structured JDs on scraped roles:
   `ALTER TABLE brsr_jobs ADD COLUMN IF NOT EXISTS sections jsonb;`

## Project Status — earlier: last updated 2026-08-01

**DEPLOY STATE (2026-08-01) — RESOLVED on 2026-09-04, kept only for history. That sprint is pushed and live; ignore the instructions in this paragraph.** A ~12-commit sprint was committed locally but not pushed. The dev machine had lost outbound connectivity to **github.com + vercel.com (and api.anthropic.com)** mid-sprint while google.com still returned 200 — i.e. a **local firewall/VPN/proxy block on those specific hosts**, not a GitHub/Vercel outage, and not DNS. So `git push` and `vercel` both fail with connection timeouts. **A new session MUST first run `git rev-list --count origin/master..HEAD` (expect ~12) and, once the machine can reach github.com again (toggle VPN / use a phone hotspot), run `git push origin master` then `$env:NODE_TLS_REJECT_UNAUTHORIZED="0"; vercel --prod --yes`.** Everything *before* this sprint was pushed + deployed live to saaksh.co during the session. The pending commits are all clean-build + verified locally (20-route Playwright smoke desktop+mobile = 0 errors; 20 unit tests pass): blog-nav unify + last-4 job JDs · jobs mobile filter sheet · clone-last-year · agency nudge · brief push quiet-hours · importer coverage note · %-validation · scanned-bill OCR (beta) · mobile job-detail sheet · clone prior-year carry · OCR failure-messaging · clone-util tests. (Untracked stray root `*.png`/`*.jpeg` screenshots and `docs/ECS-one-pager.html` are intentionally NOT committed.)

**ESG Jobs board — `/jobs` + a free headless scraping pipeline (built + mostly deployed this session):** A curated + auto-refreshed board of Indian ESG/sustainability roles (retention + a consultant top-of-funnel). **(1) Board** — `src/app/jobs/page.tsx` (desktop master-detail, widened to `maxWidth 1520`; a **mobile filter sheet** [a "Filters" button → bottom sheet reusing the desktop `FilterGroup`/`OptionRow`] and a **mobile job-detail sheet** [tapping a card opens a full-screen `DetailPane` via a new `embedded` prop] — both rendered at the *page root* to escape the `anim-up-sm` ancestor transform that otherwise breaks `position:fixed`). Data model in `src/lib/jobs.ts` (`Job`, `JobSection`, `toBullets`, and the **`useMergedJobs`** client hook in `src/lib/jobs/useMergedJobs.ts` merging curated `src/data/jobs.json` + stored `/api/jobs`, curated wins). A phone **Jobs tab lives inside the Brief** (`BriefFeed.tsx` `JobsView`/`JobRow`/`JobSheet`); saved jobs in `localStorage` (`saaksh:brief:savedjobs`). **All 18 curated jobs now carry rich structured JDs** (`sections: [{heading, body?, bullets?}]`, rendered by `src/components/jobs/JobDescription.tsx`, which prefers `sections` over the `aboutRole` fallback); each was verified live + structured from the real posting text. **(2) Free scraping pipeline** — `scripts/scrape-jobs.mjs` runs a real headless Chromium (Playwright) **inside GitHub Actions** (`.github/workflows/jobs-refresh.yml`, twice daily) → grounded Groq extract → per-link liveness verify → upsert into a new Supabase **`brsr_jobs`** table; served read-only through `/api/jobs` (`src/lib/jobs/db.ts`, best-effort → `[]` before the table exists). **No paid scraper** — Firecrawl was prototyped then dropped for the free headless-in-CI approach (user's "free scrapers only" ask); LinkedIn is deliberately excluded. Load-bearing learnings: Groq free tier = **8000 TPM per key** (a big request 413s → small prompts + `reasoning_effort:low` + 5s spacing + 6-key rotation), and a dedup `in.(...)` filter with hundreds of long URLs overflows the request line and silently fails → both the jobs scraper and the Brief now fetch the stored-URL set and intersect locally. **USER TO-DO to switch scraping on:** run the `brsr_jobs` CREATE TABLE SQL (posted in-session) — the GitHub repo secrets (`NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `GROQ_API_KEY`+`_2..6`, `APP_URL`, `CRON_SECRET`) were already set via `gh secret set` this session.

**Saaksh Brief — broadened sources + 20-min freshness + reliability + UX (this session; sources/freshness/dedup/streaming DEPLOYED, push-quiet-hours + read-more PENDING in the 12-commit batch):** (a) **Sources broadened** (`src/lib/brief/ingest.ts`) — added government/ministry Google-News queries (MoEFCC/CPCB/BEE/EPR/green-credit), market/ratings (ESG funds, MSCI/CRISIL, listed-company net-zero), new publisher feeds (ESG News, ET EnergyWorld) + **SEBI's official RSS** (keyword-gated so its non-ESG volume never spends the AI budget); relevance gate widened to accept Indian ministry/regulator moves; candidates now **round-robin per source host** so one high-volume feed doesn't flood the ~20-item/run AI budget. (b) **Freshness** — the refresh GitHub Action runs **every 20 min** now (Vercel Hobby cron is only daily); `APP_URL`+`CRON_SECRET` secrets set via `gh`, so it's live. (c) **Dedup fix** — `existingUrls` in `src/lib/brief/news.ts` (see the shared learning above). (d) **Link-preview fix** — `/brief` now streams via `<Suspense>` (feed-dependent centre + right rail behind a fallback) so the `<head>` OG tags flush in the first ~0.6s instead of after the 2-3s feed fetch; WhatsApp's preview bot was timing out and showing a bare `www.saaksh.co` card. (e) **Push quiet-hours** (PENDING) — `/api/cron/news` gates `sendPushToAll` to ~8am-10pm IST + ≤4 per rolling 3h (in-memory). (f) **Read-more link** on every card (PENDING) + mobile `fixed inset-0` phone shell to stop the iOS bottom-nav jump + re-centred pull-to-refresh spinner/"Feed refreshed" toast (framer-motion's transform was cancelling the CSS `-translate-x-1/2`).

**Collect additions (all in the 12-commit PENDING batch):** **Clone-last-year** — `cloneCampaignAction` (`actions.ts`) + a confirm-gated "Clone for next year" item in `CampaignRowMenu`: recreates a campaign's data owners + each owner's assigned fields for the next FY (silent, no emails), and **carries this year's collected figures into next year's `prior_value`** (matched by owner email + field). Pure date/period helpers `bumpYear`/`nextReportingPeriod` extracted to `src/lib/datarequest/clone-util.ts` (+8 unit tests). **Scanned-bill OCR (beta)** — new `geminiVision` (multimodal `inline_data`) in `gemini.ts` + `extractBillImageAction`: a photo of a bill/invoice/meter reading dropped into the Collect importer (`BulkImportPanel`, now accepts `image/*`) → figures mapped to BRSR fields as **verify-first suggestions** (never auto-applied), same review UI as the text importer; degrades gracefully with an `aiError` flag ("reader unavailable" vs "no figures found"). **⚠️ Gemini keys were credit-exhausted (429) at build time — OCR extraction accuracy is UNVERIFIED; test live once billing is topped up. The text importer's PRIMARY path is also Gemini (`extractFromChunk`, Groq is the fallback), so check Gemini billing.** **%-validation** — `validateItemValue` (`validation.ts`) now warns when a `%` field exceeds 100 (unit-slip guard; still soft-warn, never blocks). **Importer coverage honesty** — the live auto-fill already reads the WHOLE PDF (page-batched, concurrent; the `54000`-char cap is on dead code — `extractValues*`/`ImportPanel.tsx` — wired nowhere); the misleading "only the first part was scanned" note was replaced with an accurate coverage line.

**Agency partner nudge + blog-nav unify (PENDING batch):** a checkbox "I run an agency/consultancy interested in bundling or reselling Saaksh" on `RequestAccessForm` → `requestAccessAction` tags the stored lead (`[AGENCY / PARTNERSHIP INTEREST]` message prefix, zero schema change) + flags the founder email; a hero line invites agencies on `/request-pro`. Blog pages now use the shared `SiteHeader active="blog"` (the divergent `BlogHeader.tsx` was deleted — the third nav vocabulary is gone).

**GTM (this session, non-code): pivot to agency partnerships (from a Priya Ranjan call).** Self-serve Pro isn't the near-term money (junior staff try the tool; buyers resist paying — she confirmed webinars had to cut prices). She won't be a hands-on user but will advise + endorse + be tagged on LinkedIn. Her referral: **ECS – Environmental Compliance Services** (ecs.biz, via **Mayuri Ganguly**) — **BUT ECS already runs its own ESG platform "Redefine"**, so Saaksh is pitched as a **free BRSR funnel + delivery accelerator + Rahul as a software/BRSR consultant**, NEVER as competing software. Deliverable shipped: **`docs/ECS-one-pager.html`** (branded, open → Print-to-PDF). LinkedIn is her #1 channel (3-4 posts/wk, target the junior data-collection crowd, benefit-led captions, Brief items as the content engine, tag Priya). Full GTM plan + 6 LinkedIn drafts + the ECS one-pager copy are in the plan file `~/.claude/plans/shiny-drifting-quokka.md`. Metrics at the call: **~145 users, ~32-35 returning, 0 Pro requests.** (Discord climate servers — Climate Town, Zero Waste — are anti-commercial by culture; do NOT pitch there. Real audience: ESG Leader's Forum WhatsApp + NGOBOX consulting groups + AGSP + r/ESGIndia.)

**Saaksh Brief follow-ups — light re-theme, Kapyn-feel swipe, real images, web push (shipped 2026-07-22):** A batch of refinements on the Brief after a live review (the original 2026-07-20 entry below is now partly stale: the Brief is **light** to match the site, not dark forest, and it **does** use **framer-motion**). **(1) Light re-theme** — `BriefFeed.tsx` moved off the dark `forest` palette to the site's light tokens (`bg-page`, `ink`/`ink-body`/`ink-muted`, `brand-*`, `line`). **(2) Full Kapyn swipe feel** — replaced CSS scroll-snap-only horizontal category change with **framer-motion** drag (`useMotionValue`/`useVelocity`/`animate`, spring, adjacent-category peek, 8px direction-lock non-passive touch handling), plus pull-to-refresh, a "New since last visit" badge (`store.ts` `getLastVisit`/`stampVisit`), hero image fade-in, and IntersectionObserver active-card haptics. **(3) Phone mockup at Kapyn's exact size** — the desktop bezel was 430px (tablet-ish); now **330px screen**, `min(830px, 100dvh-88px)` tall, 38/42/52px radii, an 11px+4px two-layer device bezel (`page.tsx`), with a real **QR code** (`qrcode` dep) to open on phone. **(4) Real news images** — Google News RSS links are redirects with no `og:image` (hence gradient-only cards); added **dedicated publisher feeds** (Mongabay India, ESG Today) in `ingest.ts` that carry `media:content` images read straight from the feed item (`imageFromItem`), preferred over the page-meta scrape. Google News stays for coverage. **(5) Feed-order fix** — future-dated curated **deadlines** (e.g. a CBAM surrender due Sep 2027) no longer sort above fresh news: `feed.ts` `sortTime()` mirrors future dates below `now` by how far off they are. **(6) Top hairline** — the site-wide `body::before` brand line is hidden on `/brief` (`body:has(.brief-root)`). **(7) Tightened relevance gate** — the ingest SYSTEM prompt now has a strict India-ESG RELEVANCE GATE that drops foreign CSR reports/awards (the "Cimbali coffee" class of noise). **(8) Web push notifications** — new **`src/lib/brief/push.ts`** (store subscriptions in **`push_subscriptions`**, fan out via the new **`web-push`** dep, prune dead endpoints; no-ops without VAPID/table), public **`/api/push/subscribe`** register/unregister, **`push-client.ts`** (feature-detected subscribe/unsubscribe, hidden where unsupported e.g. non-installed iOS Safari), `sw.js` push + notificationclick handlers, a **"Notify me on big updates"** toggle in the Brief Profile view, and the **news cron sends one push only on genuinely-new items** (`insertNews` now returns the inserted rows; dedup means most runs insert 0, so a few pings/day at most). **VAPID keys generated + set in Vercel prod + `.env.local`** (`NEXT_PUBLIC_VAPID_PUBLIC_KEY`/`VAPID_PRIVATE_KEY`/`VAPID_SUBJECT`); **user ran the `push_subscriptions` table SQL**. **Verified:** clean build (86 pages), the two publisher feeds parse with real image URLs, a fresh prod ingest inserted ESG-Today items (one with a stored image), the subscribe route returns 200 gracefully, and the VAPID public key is baked into the `/brief` client bundle. Deployed to saaksh.co. **USER TESTS ON PHONE:** open `/brief` → Profile → "Turn on notifications" (grant permission) → next cron with new items pushes. **Still open:** optional GitHub secrets (`APP_URL`+`CRON_SECRET`) for hourly refresh; consider quiet-hours/rate-cap on push if it ever feels frequent.

## Project Status — earlier: last updated 2026-07-20

**Saaksh Brief — swipeable, installable 30-second ESG/BRSR digest (mobile-first PWA, shipped 2026-07-20):** A retention play modelled on the user's "Kapyn" app: an "Inshorts for Indian ESG/BRSR" at **`/brief`** to drive daily return visits + home-screen installs. **Three layers.** **(1) Content pipeline** — `src/lib/brief/ingest.ts` pulls **Google News RSS** search queries (BRSR/CBAM/CCTS/SEBI/global, in `BRIEF_SOURCES`) via the new **`rss-parser`** dep, one **grounded Groq call per item** (`groqComplete`, `openai/gpt-oss-20b`) to classify into 6 categories + write a 2-3 sentence calm summary, or return `LOW_SIGNAL`; best-effort OG-image + description scrape of the article; dedup on `source_url`; stores in a new **`brsr_news`** Supabase table (best-effort, `try/catch → no-op` before migration). Verified vs live RSS: 434 fetched → 19 kept with **faithful, no-fabrication summaries**. **(2) Feed** — `src/lib/brief/feed.ts` `getBriefFeed()` merges fresh news + the cited `regulatory_updates.json` + all 30 blog guides into one `BriefItem` model (`src/lib/brief/types.ts`), interleaved so no >2 same-category in a row. **(3) Mobile PWA UI** — `src/app/brief/{layout,page}.tsx` (own full-screen dark chrome, `viewport.themeColor`, `appleWebApp`) + `src/components/brief/BriefFeed.tsx`: a **CSS scroll-snap** vertical swipe (deliberately **no framer-motion**), `forest #0F1E33` dark palette + `ondark` AA text, category pills, a grounded **"Why it matters"** bottom sheet (public `whyItMattersAction`, cached), Saved + Profile views, a localStorage daily **streak** (`src/lib/brief/store.ts`), PWA **install** prompt; desktop shows a centred phone-frame + info + "open on phone" panel. **PWA:** `src/app/manifest.ts` + generated `public/icons/*` (navy+S) + minimal `public/sw.js`. **Refresh:** `CRON_SECRET`-gated **`/api/cron/news`** in `vercel.json` (Vercel Hobby = **daily**) + an optional **`.github/workflows/brief-refresh.yml`** (every 3h; user adds `APP_URL`+`CRON_SECRET` repo secrets to enable). **Brand guardrails (load-bearing):** curated regs + guides are **never** AI-summarised; news cards are labelled **"AI summary · gpt-oss-20b"** and tap through to the **source**; grounded prompts + a quality gate; degrades to curated-only if the table/Groq are absent. **Discovery:** "Brief" in the Resources nav (`nav-items.ts`); a dark **BriefNudge band** on the homepage (`LandingPage.tsx`); `HelpWidget`/`ConsentBanner` hidden on `/brief`. **Verified:** 5 unit tests pass, clean build, Playwright 390+1440 (swipe feed, pills, grounded Why-it-matters sheet, bookmark→Saved, Profile, 0 console errors), and **live on saaksh.co** — the first production ingest **inserted 19 news rows** and the feed shows the "AI summary" cards. Branch `feat/saaksh-brief` → master, deployed. **USER TO-DO (done 2026-07-20):** ran the `brsr_news` `CREATE TABLE` SQL. **Deferred:** web push, per-user bookmark sync, importance ranking beyond recency.

**Pre-launch security hardening of the public forms + community broadened to ESG (shipped 2026-07-02):** A two-agent security + code review before broadcasting to consultants. **Verdict: the code is fundamentally sound** (honeypot works, all email HTML `esc()`-escaped, no email-header/PostgREST/SQL injection, secrets server-only, 192-bit owner tokens, consent-gated analytics, external links `rel="noopener"`, no dead community link). **The one real gap was abuse control on the two PUBLIC forms** (`subscribeAction`/`requestAccessAction` in `src/lib/datarequest/leads.ts`) — request-Pro inserted a row AND emailed the founder on every call (Resend-quota/inbox-flood/cost vector), with no rate-limit or input caps. **Fixed (user chose the no-new-deps "recommended set"):** **(1)** field length caps in `leads.ts` (email ≤254 rejected; name/org/clients/message/source truncated via `cap()`) + matching `maxLength` on the form inputs (`RequestAccessForm.tsx`, `SubscribeForm.tsx`). **(2)** per-email dedupe on request-Pro — new `db.recentAccessRequestExists(email, sinceIso)` (24 h window, `encodeURIComponent`'d) short-circuits a repeat email to a silent success (no insert, no founder email); works across serverless instances. **(3)** best-effort global founder-email rate cap — new **`src/lib/datarequest/throttle.ts`** `allowFounderEmail()` (≤30 per rolling 10 min, in-memory/per-warm-instance) guards both `notifyFounder()` calls. **(4)** evidence upload caps in **`storage.ts`** `uploadEvidence` — `MAX_EVIDENCE_BYTES=10MB` + a MIME allowlist, and the object `Content-Type` comes from the allowlist not the attacker-supplied `file.type`; submit `accept` + a "max 10 MB" hint aligned (`SubmitItemRow.tsx`). **(5)** JSON-LD hardening — new **`src/lib/jsonld.ts`** `jsonLdHtml()` escapes `<` so a value can't break out of the `<script type=ld+json>` tag (`layout.tsx` + `blog/[slug]/page.tsx`; static data today, defense-in-depth). **Deferred (offered, not chosen):** distributed per-IP rate-limit / captcha (needs Upstash or Vercel KV), constant-time passcode/cron compare, per-user auth + RLS (the shared-passcode model). **Also:** the **community was broadened from "BRSR consultants" → "ESG consultants"** on `/community` (H1/metadata/benefit/join copy) — the user wants it to be everything an ESG consultant needs, not BRSR-only. **WhatsApp group name (user's to set, not in code):** recommended broad non-BRSR names — "Saaksh · ESG Consultants India" (top pick), "The Evidence Room · Saaksh", "Sustainability Consultants' Circle (India)", "Saaksh Collective". **Verified: 12 new unit tests pass** (leads caps/dedupe/honeypot, throttle window, jsonLd escape), clean build (56 pages), and **Playwright on saaksh.co** confirms the ESG H1, the live `maxLength` caps on request-Pro, the subscribe form still submits to success, 0 page errors. Branch `fix/public-form-hardening` → master, deployed. (Note: a `playwright-harden-test@example.com` test row now sits in `brsr_subscribers` — harmless.)

**Join-the-community (WhatsApp) + About-page vision & mission (shipped 2026-07-02):** Two additions for trust + retention (all client-side, no backend/deps). **(1) Community** — a consultant community positioned as *"the updates & know-how that don't fit in a report"* (a retention / consultant-network step). New **`/community` page** (`src/app/community/page.tsx`, server component modelled on `/about`: navy hero + a 3-card "what you get" + a WhatsApp **join card**). The join button (`src/components/CommunityJoin.tsx`, client) reads **one `COMMUNITY_WHATSAPP_URL` constant** in `src/lib/links.ts`; while it's an empty placeholder, `hasCommunityLink` makes it render an **"Invite link coming soon"** state so we never ship a dead `chat.whatsapp.com` link. **"Community" is a top-level nav link** (after Pricing) in **all three** nav surfaces (`SiteHeader`, the homepage's own hardcoded `Header` in `LandingPage.tsx`, and `MobileNav`). In-product entry points: a **"Join the community" row in the report sidebar** (`ReportView.tsx`, mirrors the "Collect" link row; new `community` `TabIcon` + `ICON_COLOR`), a row in the **Help widget** footer (`HelpWidget.tsx`, beside "Share feedback"), and a **"Community" link in both footers'** Resources columns (homepage `LandingPage.tsx` + `blog/BlogFooter.tsx`). Tracking: `community_joined { from, platform }` via the existing `track()`. Second platform (LinkedIn) reserved with a "coming soon" line + sitemap entry added. **(2) About page** — added a **Vision & Mission** section to `src/app/about/page.tsx` (after the hero): mission = put big-firm tooling in every independent ESG consultant's hands; vision = one platform, BRSR → CBAM/CCTS/ESG, collect-once-report-to-every-framework, every figure traced to source. **Deliberately DEFERRED (user's call, consent-gated):** the named **co-builder credential for Priya Ranjan** (`linkedin.com/in/priya-ranjan-5706591b`) ships **nowhere in the product** yet — she's credited only in the **manual WhatsApp welcome message** the user sends; the existing unnamed "shaped by practising BRSR consultants" line on `/about` is untouched. Fast follow once she confirms: name + LinkedIn + experience on `/about`. **USER TO-DO — paste the real WhatsApp invite into `COMMUNITY_WHATSAPP_URL` in `src/lib/links.ts`** (until then the join CTA shows "coming soon"). **Verified (clean build, 56 pages + Playwright on saaksh.co, 0 page errors):** the "Community" top-nav link renders + active-tints, `/community` shows hero/3 cards/join-card in the coming-soon state, `/about` shows Vision & Mission (and **no** Priya credential), the report sidebar + Help-widget rows both route to `/community`, and "Community" is in both footers. Branch `feat/community-and-about` → master, deployed to saaksh.co. **Local build note:** this machine OOM'd on `next build` under RAM pressure; `NODE_OPTIONS="--max-old-space-size=4096" npx next build` fixed it.

**End-to-end product test against a real assured BRSR + two honest fixes (done 2026-07-02):** Ran the full free-tool E2E the user asked for, using a real **limited-assured Thangamayil Jewellery FY25-26 BRSR** (modeled as Other/Manufacturing · listed top-1000 · 3+ yrs · EPR+Factory-Act → 108 Section-C fields). Method: Playwright against production **saaksh.co** + node harnesses against the real `report-extractor.ts`/`emissions-calculator.ts`; all on-device, nothing uploaded. **8/8 tests PASS:** gap distribution exactly as predicted (6% ready · **Ready 4 · Verify 6 · Collect 98**); **calculators reproduce the assured figures** — Total energy **38,709.0 GJ** (report 38,709.01, exact), Water **50,025.0 kL** (exact), Scope 2 **7,274.58 tCO₂e** (report 7,280.99, **−0.09%**, CEA 0.710 vs 0.7106), intensities live (4.55 GJ/cr, 5.89 kL/cr, 0.872 tCO₂e/cr); all 7 tabs render with **0 console/page errors** (Beyond BRSR CBAM "Unlikely" — no EU export); exports all work (CSV A11+B12+C108, PDF **±materiality opt-in** confirmed via pdf.js text-extract — plain has no topics, ticked has the "Suggested material topics" section, Alignment CSV **+ Word**, share-link `?v=` round-trip regenerates identical report); Ask Saaksh answered 5/5 with **0 Groq calls, 0 external hosts**; real upload of a generated report PDF tagged **0→36 "Last year"** rows and updated the card to "35 documented → 78 collect fresh". **Two fixes shipped (both client-side, no deps):** **(1)** GHG calc now shows a note under Scope 1 that it's **fuel-combustion only** — on the sample the assured Scope 1 (1,126.6 t) is dominated by refrigerant/fugitive the free calc doesn't model (it returns 136.65 t), so the note prevents a misread. **(2)** Upload detector: **SB-6 and P3-E6 primary phrases now match SEBI's actual wording** (SB-6 `"performance against the specific commitments"` → `"against the specific commitments"` + `"reasons in case the same are not met"`; P3-E6 `"mechanism to receive and redress grievances"` → `"receive and redress grievances"` for SEBI's "Mechanisms available to receive…"; node-verified they fire on SEBI-format text with no regression and no collision with the P5 human-rights grievance line). **Deliberately NOT changed:** the P6-E2 "designated consumers" match on a negated sentence — a documented "No, not a DC under PAT" is still an answered disclosure the consultant can reuse, and every match is surfaced with its snippet, so suppressing negations would make the detector *miss* real documented answers. Branch `feat/e2e-scope1-detector` → master, deployed to saaksh.co (Scope 1 note verified live). Clean build (55 pages).

**Homepage: hero alignment, comprehensive "How it works", competitor-informed nav declutter (shipped 2026-07-02):** From a hero-section review. **(1) Hero mockup pulled up** — the hero grid (`src/components/LandingPage.tsx`) was `items-center`, which vertically centred the shorter product mockup against the taller text block so it floated low; changed to `items-start` so the mockup top-aligns with the headline. **(2) "How it works" rebuilt (MECE)** — the old minimal 3-one-sentence-step block became a comprehensive, banded 4-step section ("From a blank format to a filed report."), each step a card with a numbered badge + a **"You do: … / You get: …"** split: (1) describe the client, (2) work the action plan (108 fields + calculators + upload last year), (3) report across frameworks (materiality, GRI/TCFD/IFRS/TNFD + MSCI/DJSI, CBAM/CCTS, templates), (4) hand off or go Pro. Keeps `id="how"` so the hero "See how it works" button still scrolls to it. **(3) Nav decluttered from 7 items → 4 + CTA**, grounded in a scan of indirect competitors (Indian corporate ESG SaaS **breatheESG, Lythouse, Oren** + globals **Watershed, Coolset, Linear**): **none keep "How it works" in the nav** (always a page section), all use **one product menu with internal groups** (Lythouse "Products ▾" → Modules + Capabilities) and **a single "Resources ▾"** for all content. New structure everywhere = **`Tools ▾ · Pro ▾ · Pricing · Resources ▾` + [Start free]** (homepage also keeps the Continue banner): the two tool dropdowns (Filing & audit + Tools) **merged into one "Tools ▾"** with two labelled groups (Filing & audit / Free tools, so everything stays findable); a **new "Resources ▾"** (Latest updates · Blog · Methodology & sources · About, the latter now finally on desktop) replaces the standalone Latest + Blog links; **"How it works" removed** from the nav. Mirrored across the homepage internal `Header`, the shared `SiteHeader` (menu-state type `"tools"|"pro"|"resources"`, active-tint on Tools/Resources), and `MobileNav` (Pricing + Filing & audit + Free tools + Resources sections). New shared `RESOURCES_NAV_ITEMS` in `src/lib/nav-items.ts`; the homepage Header's local arrays gained a `RESOURCES_ITEMS` copy; the unused `scrollTo` prop was dropped from `Header`. **Verified (build clean, 55 pages + Playwright 1440 & 390):** hero mockup top-aligns, the 4-step How-it-works renders, the desktop nav shows `Tools ▾ (two groups) · Pro ▾ · Pricing · Resources ▾` with **no "How it works"**, the Tools dropdown lists all 14 tools under the two group headers, Resources lists Latest/Blog/Methodology/About, and the mobile sheet mirrors it. Branch `feat/homepage-nav-howitworks` → master, deployed to saaksh.co. **Deferred (note):** the divergent `src/components/blog/BlogHeader.tsx` (a third nav vocabulary) could be aligned later. **USER FOLLOW-UP (agreed):** run a full end-to-end product test once the user provides a **text-based sample BRSR PDF** (intake→report, upload the real PDF through the on-device detector, every tab, calculators vs the report's real numbers, all exports, Ask-Saaksh) → deliver a pass/fail checklist.

**Report polish batch, readability + on-device help + Action-Plan guide + materiality-in-PDF (shipped 2026-07-02):** Six fixes from a report review (all client-side, no backend/deps). **(A) Templates readability** — the disclosure-examples `ExampleRow` and the whole **`src/components/TemplatesPanel.tsx`** used the old warm `stone-*` greys instead of the app's cool `ink` tokens (body text `stone-600/700`, labels `stone-500`), reading washed-out; migrated all content text → `ink-body`/`ink-muted` at ≥13px (body 14px, both example + explainer blocks now the same dark colour). **(B) "Upload last year's report" reframed** (`src/components/checklist/UploadCard.tsx`) — the user questioned its value; it's genuinely one of the stronger free features (turns the gap analysis from a *guess* into a *fact*), but it's an **on-device keyword scan, not AI** (pdf.js text extract + `report-extractor.ts` phrase matching). Kept it, rewrote copy outcome-led + honest ("Skip the fields you've already filed"; broadened to "last year's BRSR, an annual report, or policies"), added an after-upload **"N already documented → M to collect fresh"** count (new `remainingToCollect` prop from `DataChecklist`), and a subtle Pro on-ramp ("keyword scan, not AI, want AI to read the whole doc? That's Collect (Pro) →" → `/features/collect`), plus contrast fixes. **(C) "Ask Saaksh" is now AI-free** — it called Groq first (`help-ai.ts` → `askSaaksh`) then keyword results; **deleted `src/lib/datarequest/help-ai.ts`**, stripped the AI path/state from **`src/components/HelpWidget.tsx`** (now pure `searchHelp` keyword matching, verified **0 `api.groq.com` calls**), rebranded "Ask Saaksh AI" → **"Ask Saaksh"** ("Answers from the Saaksh help library"). Expanded **`src/data/help_topics.json`** 36 → **81** curated Q&As covering every tab/tool/Pro feature, pricing/privacy/who-it's-for, and more BRSR/CBAM/CCTS glossary. (`groq.ts` kept, still used by the narrative + importer.) **(D) Download-PDF tooltip** — the button had only a terse native `title`; added a styled `group-hover` popover explaining it's a client-ready data-request brief (gaps grouped by owning team), built on-device, full 108-field detail is the CSV. **(E) Persistent "How the Action Plan works"** — an always-available, collapsible panel (`ActionPlanGuide` in `DataChecklist.tsx`, default collapsed, grid-rows collapse) with the 4-status legend (from `STATUS_META`) + a "what you can do" list. **(F) Opt-in materiality → PDF** (user's call: give the choice) — `downloadReportPdf(report, { materialityTopics })` (`src/lib/report-pdf.ts`) appends a cited "Suggested material topics" section (grouped E/S/G, mapped to principles, honest "not a completed assessment" note) **only when** the consultant ticks a new **"Include material topics"** checkbox by the Download-PDF button; `ReportView` reads the shortlist from `localStorage` (refreshed live via a `saaksh:materiality-changed` event the Materiality tab fires) and passes only the shortlisted topics. **Verified (build clean + Playwright on a live dev report, 1440):** readable Templates, the Action-Plan guide + status legend, the reframed upload card ("Skip…"/N→M/Pro-line), the PDF hover tooltip, **0 Groq calls** from Ask Saaksh, the materiality checkbox appearing after shortlisting, and the PDF actually downloading with the section (`BRSR-data-request-tata-steel.pdf`, no page errors). Branch `feat/report-polish-batch` → master, deployed to saaksh.co.

**On-site Pro-access form + email capture + discoverability/mobile-nav fixes (shipped 2026-07-02):** Follow-up to the discoverability pass, plus an audit of that pass (the prior SEO work was done with a lighter model; two Explore agents re-checked it — **technical SEO verified correct**, the gaps were UX/discoverability + share-link hardening, all fixed here). **(1) "Request Pro access" is now an on-site form** (`/request-pro`) instead of the personal `mailto:`. New PUBLIC (no-auth) server action `requestAccessAction` in **`src/lib/datarequest/leads.ts`** stores the lead in Supabase **`brsr_access_requests`** and emails the founder (both best-effort, honeypot-guarded); `src/lib/links.ts` `REQUEST_ACCESS_URL` → `/request-pro` so all 7 CTAs (SiteHeader, PricingTable, LandingPage ×3, pricing ×2, login) pick it up. New `src/components/RequestAccessForm.tsx` (name/org/email/#clients/message + inline success) + `src/app/request-pro/page.tsx` (SiteHeader + navy hero + "what Pro adds" + form + BlogFooter). **(2) Email capture** — new PUBLIC `subscribeAction` (same file) → Supabase **`brsr_subscribers`** (falls back to a founder email if the table isn't there yet, so no signup is lost); new `src/components/SubscribeForm.tsx` (variants `card`/`strip`/`inline`, `tone` light/dark) placed at the end of each **blog post**, a strip on **/latest**, and a band in the **homepage footer**. Both actions reuse the private `sendEmail`/`esc`/`MARK_HTML` via a new exported **`notifyFounder(subject, rows)`** in `email.ts`, and a new `db.rest()` insert helper each (`addSubscriber` idempotent on the unique email; `addAccessRequest`). Mixpanel `pro_access_requested` + `newsletter_subscribed`. **(3) Audit fixes** — the homepage uses its OWN header + footer (separate from the shared `SiteHeader`/`BlogFooter`), which never linked the new pages: added **Pricing** to the homepage header nav + **Pricing** to its footer Resources + **About** to its footer bottom bar. **(4) Mobile nav** (was missing entirely — both headers are `hidden md:flex` with no hamburger): new shared **`src/components/MobileNav.tsx`** (`md:hidden` hamburger → a scrollable sheet: Explore [Pricing/Latest/Blog/About] + Filing & audit tools + Free tools + Request Pro access + Start free), rendered by both `SiteHeader` and the LandingPage `Header`. Implemented the previously-unused `SiteHeader` `active` prop (tints the current page's nav link/dropdown). **(5) Share-link hardening** — opening a `?v=` shared report no longer silently wipes a returning user's in-progress work (`report/page.tsx` now `window.confirm`s before adopting over a *different* saved form); `decodeReportParam` validates every required field; a `typeof window` guard on the share button. **No new env vars** (reuses Resend + Supabase). **USER TO-DO — run this SQL once in the Supabase SQL editor** (until then both forms degrade gracefully: access requests always email you, subscribes fall back to an email):
```sql
create table if not exists brsr_subscribers (
  id uuid primary key default gen_random_uuid(),
  email text not null unique, source text,
  created_at timestamptz not null default now());
alter table brsr_subscribers enable row level security;
create table if not exists brsr_access_requests (
  id uuid primary key default gen_random_uuid(),
  name text, organisation text, email text not null, clients text, message text,
  created_at timestamptz not null default now());
alter table brsr_access_requests enable row level security;
```
Note: reliable capture is the Supabase rows; founder-email notifications are best-effort until the Resend sending domain is verified. **Verified:** clean build; the hardened `decodeReportParam` unit-checked; **live on saaksh.co (Playwright at 390 + 1440):** the mobile hamburger opens the full sheet (Pricing/About/tools/Start free), `/request-pro` renders + the Pro nav item highlights, and the subscribe action returns success end-to-end (one test founder-email fired since the table isn't created yet). Branch `feat/leads-and-audit-fixes` → master, deployed. **Reddit:** a ready-to-paste r/ESGIndia launch post lives in the plan file (`~/.claude/plans/shiny-drifting-quokka.md`).

**Discoverability pass — SEO foundations, /pricing, /about, shareable report links (shipped 2026-07-02):** A top-of-funnel/discoverability push (the product is strong but was leaking at the top: no rich social cards, no structured data, tool pages using the generic root `<title>`, no standalone pricing or about page, and a free report that couldn't be shared). Prompted partly by a competitor spotted building the same thing on **r/ESGIndia** (reddit.com/r/ESGIndia/comments/1tgrkuo) — the right launch community, and Saaksh's edge is that it's already shipped. **(1) SEO technical** — `src/app/layout.tsx` root metadata gained `metadataBase: new URL("https://saaksh.co")`, a **title template** (`%s | Saaksh`), and minimal `openGraph` (`type`/`siteName` only) + `twitter` (`card`) blocks that deliberately DON'T pin title/description/images, so each page's card falls back to its own title/description and inherits the OG image. A **dynamic OG image** at `src/app/opengraph-image.tsx` (`next/og`, edge runtime, navy 1200×630 branded banner) is auto-injected by the file convention as `og:image` on the root **and every child route that doesn't override it** (verified: pricing/about/tools all resolve `https://saaksh.co/opengraph-image?<hash>`). **JSON-LD** — `Organization` + `SoftwareApplication` graph inline in the root layout body (logo → `/icon.svg`), and a `BlogPosting` graph on each `blog/[slug]` page. **Per-tool titles** — each `/tools/*` page is a client component, so a thin server **`layout.tsx`** per tool (8 of them) exports a unique `metadata.title` + `description` (e.g. "BRSR Core Audit Readiness Checklist", "Scope 3 GHG Calculator for BRSR P6"). Blog `generateMetadata` now sets per-post `og:image` (the existing `/blog/<slug>.jpg` cover, fallback to the dynamic OG route) + a `twitter` block. `src/app/sitemap.ts` gained all 8 `/tools/*` + `/pricing` + `/about` + `/latest`. **(2) `/pricing` page** — the Free vs Pro tier cards were extracted from `LandingPage.tsx` into a shared **`src/components/PricingTable.tsx`** (`TierCards`, `FREE_FEATURES`, `PRO_FEATURES`; the `onStart` prop is now optional — a button on the homepage, a `Link` to `/start` on the standalone page), so the two never drift. New `src/app/pricing/page.tsx` (server component: navy hero + `TierCards` + a 5-item FAQ + closing CTA); "Pricing" added to `SiteHeader` nav + `BlogFooter` Resources. **(3) `/about` page** — a credibility page (the problem → the approach → principles → who built it, Rahul + LinkedIn/email + consultant validation); "About" added to `BlogFooter` Company column. **(4) Shareable report links** — a free report lived only in `localStorage`, so it couldn't be sent to a colleague. New `encodeReportParam`/`decodeReportParam` (url-safe base64 of just the intake form, unicode-safe via `TextEncoder`, validates shape) + `adoptSharedForm` in `src/lib/storage.ts`; a **`ShareLinkButton`** (`src/components/SessionBackup.tsx`) in the report sidebar copies `${origin}/report?v=<param>` (clipboard, prompt fallback; Mixpanel `report_link_copied`); `src/app/report/page.tsx` reads `?v=` from `window.location.search`, adopts the shared form (clearing the prior client's checklist), and strips the query via `history.replaceState`. Still 100% on-device: the recipient's browser regenerates the report, nothing is uploaded. Also removed the dead `COMPLIANCE_CHAT` const left over from the earlier Compliance-Chat removal. **Verified:** clean build (56 pages); the base64url round-trip unit-checked (url-safe, unicode company names preserved, garbage/wrong-shape → null); built-HTML inspection confirms per-page `og:title`, inherited `og:image`, JSON-LD types, per-tool titles, sitemap contents, and **zero** broken `opengraph-image.png` references. Branch `feat/discoverability-seo` → master, deploying to saaksh.co. **USER TO-DOs (no-code, high-value):** post to **r/ESGIndia** as the already-shipped tool + comment on the competitor thread; submit the sitemap to Google Search Console; check the OG card via LinkedIn Post Inspector. **Deferred (next):** email capture → Resend audience; upgrade `REQUEST_ACCESS_URL` from `mailto:` to a Tally form; content-SEO blog posts (per-principle "How to fill BRSR P1–P9", more sector guides).

**Readability + outcome-led craft pass, "Latest" feed, direct feedback (shipped 2026-07-01):** A consultant-perspective polish pass (the user flagged: text too light / too small, tool pages felt like walls, no return hooks, no feedback path). **(1) Readability system** — the `ink.faint` token was darkened `#6B7585 → #616A78` (lifts every small label app-wide), `--ink-faint` + `--ink-on-navy` CSS vars added; rule now: content/secondary/CTA text is ≥13px and never lighter than `ink-muted`, `ink-faint` reserved for ≤11px uppercase labels; tool-hero subtext on navy brightened to `#C4D0E0` (AA on `#0F1E33`). ~29 `text-ink-faint`/small-font offenders on the four tool pages were bumped. **(2) Outcome-led framing** — new shared **`src/components/tools/ToolHero.tsx`** (eyebrow + outcome-led H1 + readable subtitle + a green-check "what you get" benefits strip + a "who/when" line); all four `/tools/*` pages now lead with the payoff. **(3) Layout/flow** — two-pane (modelled on `ppp-intensity`'s sticky sidebar): **audit-readiness** got a sticky left **principle jump/filter rail** (P1–P9 + counts + "Core only" + CSV) that kills the ~4000px wall; **xbrl-preflight** puts the converter sticky-left with the checklist right; **wellbeing-schedule** gets a summary/export rail beside the table; all stack on mobile. **(4) "Latest" feed** (habit hook) — new `src/data/regulatory_updates.json` (8 dated, cited SEBI/CBAM/CCTS items, `{id,date,display_date,tag,title,summary,source_label,source_url}`), `src/lib/latest.ts` merges it with `BLOG_POSTS` into one date-sorted `LatestItem[]` feed (`topLatest(n)`, `LATEST_TAGS`), a new **`/latest`** page (tag-filterable, coral "Update" cards link to the primary source, blue "Guide" cards link to the blog post) + a homepage **"Stay ahead of the regulation"** strip (between `FilingAuditBand` and `ProductPreviewSection`); "Latest" added to `SiteHeader` + the `LandingPage` internal `Header` + the `BlogFooter` Resources; Mixpanel `latest_item_clicked`. **(5) Direct feedback** — `FEEDBACK_URL` (prefilled mailto) in `src/lib/links.ts`, surfaced in the **HelpWidget** footer ("Something we could do better?") and the **BlogFooter** bottom bar ("Share feedback"); Mixpanel `feedback_opened`. All on-device, no new deps/backend. **Verified live (Playwright, 1440 + 390 + saaksh.co):** all readable heroes + benefit strips, the sticky rails/two-pane on all four tools, `/latest` (23 cards = 8 regulatory + 15 blog) + homepage strip + nav "Latest", feedback resolves to the prefilled mailto, mobile stacks, zero console errors. Clean build (52 pages); branch `feat/readability-latest-feedback` → master, deployed to saaksh.co.

**"Filing & audit tools" category + four on-device consultant tools (shipped 2026-07-01):** A new **leading top-nav category** ("Filing & audit tools", the FIRST dropdown in both `SiteHeader.tsx` and the `LandingPage` internal `Header`, wired via `FILING_AUDIT_ITEMS` in `src/lib/nav-items.ts`) plus a **homepage band** ("Built for ESG consultants", heading "The last mile: get it filed, get it assured.", asymmetric anti-slop layout: a primary blue `bg-[#EAF4FE]` XBRL card + three compact `divide-y` rows), inserted between the Trust strip and `ProductPreviewSection`. Four **standalone, 100%-on-device, cited, free `/tools/*` pages** (each: `SiteHeader` → navy hero → body → `BlogFooter`): **(1) `/tools/xbrl-preflight`** — a Lakh/Crore → absolute-INR converter (`src/lib/xbrl-units.ts`) + a 7-item cited checklist of common BRSR-XBRL rejection reasons (`src/data/xbrl_preflight.json`); explicitly **not** a taxonomy validator. **(2) `/tools/audit-readiness`** — per-KPI list of the source docs a BRSR Core reasonable-assurance auditor demands, grouped P1–P9, 9 rows flagged as the BRSR Core attributes, principle filter + "Core only" + CSV export (`src/lib/audit-readiness.ts`, `src/data/audit_readiness.json`). **(3) `/tools/ppp-intensity`** — restates an emissions/energy intensity against **PPP-adjusted turnover** so an Indian figure compares like-for-like with global peers; World Bank PPP factor (`PA.NUS.PPP`, India 2024 = **20.45**) shipped as an **editable** default in `src/data/ppp_factor.json` with a vintage caveat (`src/lib/ppp-intensity.ts`). Identity: `PPP intensity = metric ÷ (revenue₹ ÷ PPP factor)`; the results copy explains it in absolute terms (an earlier "= rupee intensity × PPP factor" caption was corrected — it's off by 10× when rupee intensity is shown per-crore and PPP per-million-int'l-$). **(4) `/tools/wellbeing-schedule`** — a downloadable CSV mapping the 11 P3 welfare heads to the P&L ledger lines they come from + a cited guide (`src/lib/wellbeing-schedule.ts`); **no** spend-to-emission mapping (honours the deferred-Cat-1 stance). All cited in house style (publication + section/version + URL + vintage), CSV via the formula-safe `downloadCsv`, no new dependencies. **Verified live (Playwright, localhost + saaksh.co):** all four routes 200 + H1 render, "Filing & audit tools" is the first nav item, homepage band renders, XBRL converter (5 cr → ₹5,00,00,000), PPP math (1000 tCO₂e / ₹500 cr / 20.45 → 4.09 tCO₂e per M int'l $, rupee intensity 2.00/cr), CSV header rows match spec, zero console errors. Clean build; branch `feat/filing-audit-tools` → master, deployed to saaksh.co. **Note:** this commit also bundled other pending working-tree work (blog system, homepage iterations, calculator/PDF/email polish, and the `brsr-consultant-kit.vercel.app → saaksh.co` redirect in `next.config.mjs`) at the user's request, so master now reflects all of it. Live domain is **saaksh.co**.

**Free tool: engagement timeline, BRSR changes guide, disclosure quality examples (shipped 2026-06-29):** Three more free features in the Templates tab, all 100% on-device. **(1) Engagement timeline generator** — new `src/lib/engagement-timeline.ts` (7 milestones, two plans: first-time 20 weeks / experienced 12 weeks, each with name/weeksBefore/owner/description; `computeTimeline(deadline, firstTime)` subtracts weeks from the filing deadline; `timelineCsvRows()` for CSV export; `defaultDeadline()` returns next Sep 30). Rendered in a new `<TimelineSection>` inside `TemplatesPanel`: a date-picker + a "First-time / Experienced" pill toggle, then a 4-column table (Milestone, Due by, Lead, Notes) + CSV download. **(2) "What's new for BRSR in FY 2025-26"** — a 4th how-to guide in the existing `GUIDES` array (pure authored content): BRSR Core assurance glide path (top 500 this year, top 1000 FY26-27), value-chain disclosure prep (FY26-27), Scope 3 voluntary but expected, SEBI March 2025 amendments already in the Saaksh KB. Cited to SEBI circulars. **(3) Disclosure quality examples** — new `scripts/generate-quality-examples.mjs` (mirrors the field-explainer generator: same Groq key rotation, same resumable pattern, same model `openai/gpt-oss-20b`; different SYSTEM prompt focused on "what a complete, assurance-ready answer includes" with one common gap per field; no fabricated figures). Generated all 108 Section-C fields in one run (zero failures); output is `src/data/brsr_quality_examples.json`. Surfaced in a new `<DisclosureExamplesSection>`: P1–P9 filter pills + a text-search box narrow the list; each field row expands to show "WHAT A COMPLETE ANSWER INCLUDES" (from the new JSON) + "IN PLAIN ENGLISH" (the existing field explainer) side-by-side; single-accordion pattern (openId state). **Verified live via Playwright:** engagement timeline renders and correctly computes dates (12-week experienced plan → kickoff 08 Jul 2026 = 12 weeks before 30 Sep 2026), Experienced/First-time toggle live-updates the table; "What's new for FY 2025-26" guide visible and expands; quality examples P1-E1 expands showing both "What a complete answer includes" and "In plain English" blocks; P6 filter pills and search box present. Clean build; branch `feat/free-templates-round2` → master, deployed to saaksh.co.

**Analytics (Mixpanel) + free tool: internal request emails & who-owns-what map (shipped 2026-06-29):** Two things. **(1) Mixpanel product analytics** — added alongside GA4/Vercel (GA4 is page-level, doesn't answer product-behaviour questions). `src/lib/mixpanel.ts` is an SSR-safe, lazily-imported wrapper (`initMixpanel(superProps)`, `identifyUser(id, profile?)`, `track(event, props)` — `track` awaits init so early calls aren't dropped); `src/components/MixpanelProvider.tsx` (mounted in `layout.tsx`) inits once with autocapture + 100% session replay, registers a `plan_type` super-property (free vs pro by path), and `people.set`-identifies Pro users (`/requests`,`/submit`) as `saaksh-consultant` (shared-passcode, so one id for now). Events: `report_generated` (industry/sector/companySize/maturity/gapCount), `pdf_downloaded`, `autofill_completed` (figuresFound/chunks/confidence), `suggestions_applied`. Token is the public browser token (hardcoded). **Verified in-browser (Playwright):** `api-js.mixpanel.com/track` + `/record` both return 200 on saaksh.co (Events + Replays light up in the Verify-Connection screen; Users needs the identify path, which the `/requests` login flow triggers). **(2) Two new FREE features in the report's "Templates" tab** (now "Templates, emails & guides") at the start-of-engagement phase: **internal request emails** — one curated, personalized (client name + computed reporting FY) copy-paste email per owner team (HR, Plant/EHS/Energy, Company Secretary, Procurement, CSR), each a plain-English 5–8-item ask, "Copy email" → clipboard; honestly framed as the manual version of what **Collect (Pro)** automates (sending/tracking/reminders). And a **"who owns what" map** — the principle→team lookup surfaced as a reference list + a CSV download. The canonical `OWNERS` map + `reportingFy` were **extracted** from `report-pdf.ts` into a shared `src/lib/brsr-owners.ts` (the PDF imports it now — no behaviour change; `ownerMapRows()` added for the CSV); new pure `src/lib/request-emails.ts` (`TEAM_EMAILS` + `buildTeamEmail`); `TemplatesPanel` takes `clientName`, `ReportView` passes `report.companyName`. **Decisions (user):** emails are curated+personalized, not a dump of this client's gaps (the PDF brief already does the full per-team gap dump); delivery is copy-to-clipboard, not file download. Homepage Free section + compare table updated to list the new free items. **Verified live via self-screenshot + clipboard read (Playwright):** both sections render under Templates/Guides on a Tata Steel report, "Copy email" puts the right subject+body (named client + "FY 2025–26" + plain-English asks) on the clipboard, no console errors. Clean build; branch `feat/free-request-emails` → master, deployed to saaksh.co. **USER TO-DO (optional):** in Mixpanel, build the two funnels — `report_generated`→`pdf_downloaded` (free) and `autofill_completed`→`suggestions_applied` (Pro).

**Pro polish — company logos, autocomplete create-flow, owner dates trail, editable deadline, readiness consistency, em-dash scrub (shipped 2026-06-27):** Follow-up polish from a live review. **(1) Company logos** — `CompanyAvatar` (now a client component) shows a bundled `/logos/{file}.png` for known clients via `src/lib/company-logos.ts` (lenient name match; Tata Motors wired) with a monogram fallback on `onError` so a missing file never renders broken. **(2) New-collection autocomplete** — `requests/new/page.tsx` is now a client component using `CompanyAutocomplete` (hidden `clientName` input feeds the unchanged `createCampaignAction`); creating redirects to `?view=autofill` so existing documents can be dropped at once (+ a hint on the form). **(3) Owner dates trail** — `received_at` added to `brsr_contacts`; `db.markReceived` stamped in `submitDataAction` when a contact completes; `emailStatus()` now shows "Sent {date} · Received {date}" (the sent date shows even pre-migration). **(4) Editable deadline + reporting period** — `db.updateCampaign` + `updateCampaignAction` + an inline "Edit" popover on the workspace header (`CampaignSettingsButton`). **(5) Readiness consistency** — Pro `ReadinessView` now reads over the **108 Section-C disclosures** (same scale as the free report) with Ready/Collect language + an explainer that the free tool *predicts* from filings while Pro *reflects* collected data; Sections A & B shown separately, the 7 P6 activity inputs excluded from the headline. **(6) Em-dash scrub** — 447 em-dashes used as comma substitutes replaced with commas across the user-facing source (`src/**` excluding `data/` cited JSON + code comments swept too); the existing sample campaign's stored name keeps its em-dash (DB data, not code). **USER TO-DOs:** drop `public/logos/tata-motors.png`; run `ALTER TABLE brsr_contacts ADD COLUMN received_at timestamptz;`. **Verified live via self-screenshot:** the autocomplete create form + auto-fill hint, the 108-Section-C readiness + predicted-vs-collected explainer, the header "Edit" button, and the owner "Emailed today" sent line. Clean build; branch `feat/pro-polish` → master, deployed to saaksh.co.

**Pro IA overhaul — overview dashboard, ONE "Auto-fill" space, URL-addressable sections, full-width everywhere, honest copy, CBAM upload (shipped 2026-06-27):** A from-first-principles IA pass after the user found the auto-fill confusing, the layout unfinished, and the landing over-promising. **Key reframe (the engine already did this; the UI hid it):** there is ONE upload space — the consultant uploads ANY client document and the AI **auto-distributes** its figures across all 108 BRSR fields (`extractValuesBulk` runs every doc against the full 138-field skeleton; the document category is only an OPTIONAL accuracy hint via `scopeCandidates`); that one pool feeds the **Data** tab, the **Readiness %**, **Emissions** + the **draft**. So this pass: **(1)** reverted the importer to **upload-FIRST** (`BulkImportPanel` — "upload anything, the AI sorts it across all 9 principles"; the category-first picker from the prior pass was REMOVED, category is now an optional per-staged-file dropdown), moved it OFF the page-top into a new **"Auto-fill" workspace tab** (`CampaignWorkspace` — company header now comes FIRST), and **removed the duplicate** older single-doc `ImportPanel` from the workspace "Tools" — so there is exactly ONE upload space. **(2)** Workspace views are now **URL-addressable** (`?view=`, read via `useSearchParams`; tab clicks use `history.replaceState` for refetch-free switching; a sidebar deep-link is picked up by an effect), so the **sidebar `CampaignRow` dropdown lists each client's sections** (Overview · Auto-fill · **Readiness** · Owners · Data · Emissions · Draft) as deep-links — Readiness is its own entry (replacing the old mislabeled Owners/Data pair). **(3)** `/requests` is now a **Pro overview dashboard** (4 cross-client tiles computed from `listCampaigns()` — clients / data-collected % / awaiting / ready-to-draft — + the collections list, full-width); a **"Dashboard"** item (`IconGrid`) at the top of `CollectNav` makes it always reachable; the bottom Tool→"Readiness" free-tool link was renamed **"Free readiness tool"** to remove the name clash with the per-client Readiness view. **(4)** **Full-width sweep** — `requests/[id]` + `requests/cbam` dropped their `max-w` caps (edge-to-edge to the shell padding), `requests/page` dashboard fills, proposal/profile widened (`1280`/`1100`). **(5)** **CBAM upload-to-autofill** — new `src/lib/datarequest/cbam-extract.ts` (`extractCbam`, grounded extract-only, mirrors the BRSR importer) + `cbamExtractAction` read the covered good + production/export quantity from an uploaded report and fill the calculator (`CbamCalculator` gained a "Choose document" control + a verify note). **(6)** **Honest copy** — `/start` hero "Generate your client's BRSR Readiness Report in under a minute" → **"Your client's BRSR, gap-analysed and cited, in seconds"** (the gap analysis is instant; the full readiness collection is real work — no over-promise). **Verified live via self-screenshot at 1920px:** the dashboard tiles, the header-first workspace, the Auto-fill tab with upload-first copy + the one-pool explainer, the sidebar listing Readiness as its own section, and full-width CBAM + its upload control. Clean build; branch `feat/pro-clarity` → master, deployed to saaksh.co. **(Supersedes the category-first importer from the prior "Pro hardening" entry below — that picker was reverted to upload-first.)**

**Pro hardening — category-first importer, full-width workspace, delete collections, consultant profile (shipped 2026-06-27):** A table-stakes + wedge-legibility pass from a live review (the user asked for PM-grade judgment, not just execution, citing competitor tools — GreenStitch / Gensuite / Sustainability Cloud). **(1) Importer is now category-FIRST** (`BulkImportPanel.tsx`): a prominent "What are you uploading?" picker (7 typed cards each with a "what it reads" hint — Last year's BRSR / Annual report / **Energy & fuel bills → "electricity & diesel → your Scope 1 & 2 calc"** / HR & headcount → "P3, P5" / Water & waste / Policies / "Not sure / mixed") is the FIRST decision; picking a type opens the file dialog and stages the file pre-tagged (the category scopes which BRSR fields the AI extracts via `scopeCandidates()` — energy→P6 inputs, hr→P3+P5). Adds a one-line explainer that applied values become this client's **collected data** shown in the **Data** tab + the **Readiness %** — resolving the user's "is Choose-PDFs for readiness or collection?" confusion (it's one pool, three views; auto-fill is collection-from-documents). The staged cards keep a per-file dropdown to re-tag. No server/AI change. **(2) Full-width workspace** — `requests/[id]/page.tsx` content cap `max-w-[1280px]`→`[1600px]` so the Data tab + all views fill wide screens (the dead left/right whitespace). **(3) Delete collections** — `db.deleteCampaign()` (FK-safe child-first delete via PostgREST: items→contacts→directory→campaign, **no migration needed**) + guarded `deleteCampaignAction()` (`revalidatePath`) + a per-card **"⋯" menu** (`CampaignRowMenu.tsx`, native-confirm-gated, lives outside the card `<Link>`) on the collections list, so duplicate/test campaigns can be cleaned up. **(4) Consultant profile (on-device)** — new `/requests/profile` page (`ProfilePanel.tsx`) + a "Profile" item in a new "Account" group in `CollectNav`; `src/lib/datarequest/profile.ts` stores a `ConsultantProfile` (name/firm/email/phone/website + a default rate card) in `localStorage` (auth is a **shared passcode** — no per-user accounts — so on-device, reusing `storage.ts`). It **seeds the proposal fee builder's rates** (`ProposalBuilder` hydrates from the profile on mount) and **brands the proposal PDF** (`proposal-pdf.ts` gained a `preparedBy` param → a "PREPARED BY {name}, {firm} · email · phone" block). **Deferred (locked):** ₹/report billing (price to be set with Priya post-call); archive collections (a 1-column SQL follow-up after delete); per-user Supabase Auth (the eventual server-side home for the profile). **Verified live via self-screenshot (`_shoot.mjs`):** the category-first picker, the full-width Overview, the per-card delete menu, and the Profile page + Account nav all render on saaksh.co. Clean build; branch `feat/pro-hardening` → master, deployed.

**Pro overhaul — Vivid Blue + Coral re-theme, Pro workspace dashboard, AI document auto-fill, + v3 polish (shipped 2026-06-27):** A major Pro push ahead of the paid consultant demo. **(1) Re-theme Evergreen & Ember → "Vivid Blue + Coral"** (the user rejected the sustainability-green cliché and indigo/"AI-slop"): a `tailwind.config.ts` + `globals.css` token redefinition cascaded app-wide — `forest`→deep navy `#0F1E33` (sidebar rail / dark surfaces / dark sections), `brand`→a blue scale (accent `500 #1E9DF2`, primary `600 #0B6FD4`, label-safe `700 #0B5FB0`), `ember`→warm coral `#F2674A` (awaiting/collect status + pops), near-white canvas `page #FBFCFE`, whisper-blue `tint #EAF4FE`; `.font-display` is now **bold Hanken sans** (the Newsreader display serif is retired from product UI). RGB/hex constants in `report-pdf.ts`, `email.ts`, `CompanyAvatar.tsx`, the charts + CBAM + proposal surfaces were all swept to the new palette. **(2) Pro workspace dashboard** — new `src/components/datarequest/CampaignWorkspace.tsx` replaces the flat campaign page with a data-rich, full-width **multi-view tabbed workspace**: **Overview** (status donut + emissions + assurance tiles), **Readiness** (see #5), **Owners**, **Data** (expandable per-field drill-down), **Emissions**, **Draft**. **(3) AI document auto-fill — the Pro wedge** — new `BulkImportPanel.tsx` + `importer.ts` `extractValuesBulk(docs, candidates)` + `actions.ts` `bulkImportAction`/`applyBulkImportAction`: the consultant uploads the client's existing documents (last year's BRSR, annual report, bills, policies), **tags each PDF's category** (per-file dropdown: Last year's BRSR / Annual report / Energy bills / HR / Water & waste / Policies / Other → `scopeCandidates()` narrows the fields per category), each file's text is extracted **in the browser** (pdf.js, never leaves the device), and a grounded Groq pass pre-fills the BRSR skeleton as **reviewable, source-cited suggestions** — a verifier drops any value not literally present in the source, nothing auto-applies (amber "verify before applying", only ticked rows write via `getOrCreateImportContact` + `addItemWithValue`). `loadSampleClientAction()` seeds an idempotent demo campaign (3 owners incl. P6-E1-elec/diesel) so the demo always has data. **(4) Groq fix (AI is now live)** — the `keys()` validator in `groq.ts` was tripping on whitespace/quotes in the env values (pasted from a table), so the `gsk_` check silently disabled **every** AI feature; now it trims + strips wrapping quotes before validating. Verified live: Ask Saaksh + narrative + importer return grounded output. **(5) v3 polish (this session):** **Ask-Saaksh greetings** — `help-ai.ts` SYSTEM prompt now answers a greeting / thanks / vague opener ("hi", "what can you do", "help") with a warm ONE-line offer of what Saaksh helps with, instead of "I don't have information" (grounded no-fabrication kept for real questions); **upload-category prominence** — `BulkImportPanel` header copy + a clearer "Tag each document's type" heading surface the category step up front; **Pro "Readiness" view** — `CampaignWorkspace` compares the campaign's collected values against the **full BRSR skeleton** (`REQUEST_FIELDS`, 133 fields) → **covered vs missing %**, grouped Section→Principle with per-principle progress bars + a "show only the gaps" toggle (purely computed from real collected values — no fabrication — so an auto-fill or owner submission visibly moves the readiness needle). **Packaging decision (locked):** the free tool stays **100% on-device** (gap analysis + keyword detection = the funnel + the privacy promise); **Pro = "upload your documents, Saaksh fills the BRSR for you"** (AI auto-collect → readiness → draft). Per-report billing (Razorpay, ₹100/report micro-tier) is **deferred**. **Verified live via self-screenshot (Playwright npm lib, since the MCP tool was disconnected — temp script `_shoot.mjs` at repo root, untracked, do NOT commit):** blue+coral theme + Pro workspace tabs + Ask-Saaksh "hello" greeting + the Readiness view (sample at 7/133 = 5% covered, P6 partially filled). Clean build; branches `feat/*` merged → master, deployed to saaksh.co. **Demo packaging strategy + walkthrough are the remaining prep.**

**Motion polish pass — Tier 1/2/3 (shipped 2026-06-26):** A curated, on-brand animation pass from an "animation vocabulary" review, kept calm/editorial (purposeful only; explicitly rejected bounce/wiggle/marquee/float/3D/ripple/confetti/typewriter, and no tickers on live-recompute figures). **Tier 1:** the report Overview readiness gauge now **counts up** (number ticker) in sync with the ring filling on mount — new `src/components/AnimatedNumber.tsx` (rAF ease-out, `tabular-nums`, `prefers-reduced-motion`-safe; `t` clamped ≥0 so it can't dip negative on frame 1). **Tier 2:** `AnimatedNumber` extended to the **Alignment** stat cards + the Collect **emissions** figures (`Metric` now takes a number) + **assurance** stat chips; **skeleton loaders** (new `Skeleton.tsx` + `requests/loading.tsx` + `requests/[id]/loading.tsx` for the Supabase-fetch window, and a workspace-shaped skeleton for `/report` instead of a spinner); **stagger** on the Overview + Alignment stat cards (above-the-fold, plays as watched); **smooth collapse** for the Templates how-to guides (controlled `GuideItem` with the grid-rows `0fr→1fr` pattern, replacing native `<details>` snap); **scroll-reveal** on the landing via new `src/lib/useScrollReveal.ts` (IntersectionObserver; only hides BELOW-the-fold `[data-reveal]` so content is never gated/flashed; reveals on scroll-in OR when leapfrogged by a fast/anchor jump; no-op under reduced-motion) on the FeatureRows + how-it-works + stats band. **Tier 3:** `CompanyAutocomplete` dropdown grows from the input (origin-aware `.dropdown-in`); **invalid-form shake** on the login card (wrong passcode) + the Add-owner error box. All CSS effects are auto-covered by the existing global `prefers-reduced-motion` rule; new keyframes (`shimmer`/`shake`/`dropdownIn`/reveal) live in `globals.css`. **Verified in-browser (Playwright):** scroll-reveal hides below-fold + reveals all on scroll incl. a leapfrog jump, visible by default; login `animationName=shake`; dropdown `animationName=dropdownIn` top-origin; Alignment cards count up to finals with stagger; Templates guide height animates `grid-template-rows` 0→236px; clean build; live smoke (`/`,`/report`,`/login` 200). Skeletons + the gated Collect tickers verified by build + code review. Branch `feat/motion-polish` → master, deployed.

**Pro: proposal & fee builder (shipped 2026-06-26):** Roadmap Phase 6. A Pro tool under the passcode-gated `/requests/proposal` (new "Win work" group in `CollectNav`) that helps a consultant **scope + price** an engagement: enter the client's scope + your own rates → a **live, transparent fee breakdown** + a downloadable branded **proposal PDF**. Fully client-side (no Supabase), nothing stored. **Honesty stance:** no public benchmark exists for Indian consultant fees, so the tool **never asserts a market price** — it structures the consultant's **own** rates (editable, "set your own numbers") via a fully-itemised rubric (base × size-complexity × first-filing multipliers + per-framework / Scope 3 / value-chain / assurance add-ons); the proposal says "a starting estimate from your inputs — adjust to your market." New `src/lib/proposal.ts` (`FeeInputs`, `estimateFee`, `buildProposalSections` — all pure), `src/lib/proposal-pdf.ts` (`downloadProposalPdf`, jsPDF mirroring `report-pdf.ts`), `src/components/datarequest/ProposalBuilder.tsx` (form + live sticky estimate), `src/app/requests/proposal/page.tsx` (`requireConsultant` defence-in-depth). **Packaging decision (user asked whether to move CBAM/CCTS/etc. to Pro): NO — keep the on-device prepare/know modules (Scope 3, cross-framework export, CBAM/CCTS readiness, templates) FREE** (research says consultants expect them free; they're the funnel; zero marginal cost). The free/Pro line is **free = understand & prepare on your device; Pro = the workspace that does the work** (Collect, importer, assurance ledger, AI narrative, proposal builder). Premium depth (CBAM/CCTS liability calculators, Scope 3 + supplier collection) is added *in Pro* on top of the free readiness, not by moving the free modules. `LandingPage` Pro pillar "Proposal & fee builder" flipped Coming → "Available now". **Verified:** rubric probe (top-1000 first-time base ₹2,10,000; first-time > repeat; +GRI+CBAM+assurance = ₹3,10,000; unlisted repeat = ₹75,000) + sections grow with scope; clean build (route 4.74 kB); gated-render smoke — `/requests/proposal` redirects unauth (307→/login), authed renders the builder with the correct SSR default total (₹2,10,000), no errors. **Not browser-PDF-verified** (won't type the production passcode into a browser) — interactive recompute + opening the proposal PDF is the manual pass; the PDF is a pure mirror of the shipped `report-pdf.ts`. Branch `feat/proposal-builder` → master, deployed. **Roadmap: Phases 1-6 shipped; only Phase 7 (accounts + billing + multi-client workspace — Supabase Auth + Razorpay) remains, deliberately last (do it once a few consultants are paying manually).**

**Free report: "Templates & guides" library (shipped 2026-06-26):** Roadmap Phase 5. A small, free, on-device library of the formats + how-to guidance consultants otherwise hunt for — a new **"Templates"** item in the report's **Reference** sidebar group (next to Sources). Shipped **free** (not Pro): `docs/pro-strategy.md` found consultants *expect templates to be free*. New `src/components/TemplatesPanel.tsx`: (1) three **downloadable templates** built in-browser via the formula-safe `downloadCsv` — **BRSR response workbook** (all 108 Section-C fields + Sections A/B as a blank fill-in sheet, generated from `brsr_data_points.json`), **Materiality assessment grid** (impact × stakeholder-concern scoring + worked examples), **Stakeholder engagement plan** (pre-filled with the six BRSR stakeholder groups); (2) three **cited how-to guides** (accordion) — running a materiality assessment, answering Principle 5 (Human Rights), getting ready for BRSR Core assurance — each with source links (SEBI, UNGPs). Honest framing ("starting formats, not a finished assessment"). `ReportView` got a `templates` reference tab (outside `TABS`, mirroring `sources`). **Verified:** clean build; **full in-browser run (Playwright)** — opened Templates, confirmed 3 template cards + 3 guides render, downloaded the BRSR response workbook and checked the CSV (header + 11 Section A / 12 Section B / 108 Section C rows, blank fill-in columns, UTF-8 BOM); screenshot reviewed. Branch `feat/templates-library` → master, deployed. **Roadmap: Phases 1-5 shipped; next is Phase 6 — proposal & fee builder (the first genuinely Pro/win-the-work item), then accounts+billing last.**

**Free report: "Beyond BRSR" tab — CBAM + CCTS readiness modules (shipped 2026-06-26):** Roadmap Phase 4. A new on-device **"Beyond BRSR"** tab in the free report with **cited readiness checks** for the EU **CBAM** and India's **CCTS** — who's in scope and what to prepare. **Lightweight by design:** applicability verdict + dated timeline + readiness checklist + cited sources, **NOT a liability calculator** (embedded-emissions / GEI-gap quantification is deferred). Derived from the intake the consultant already fills (`industry` + `exportMarkets`): **CBAM** verdict needs a covered-good sector AND an EU export market; **CCTS** is sector-based (the 9 notified GEI-target sectors, incl. **Textile**). Verdicts are "Likely applies / May apply / Unlikely", each with a plain reason. New `src/data/cbam_readiness.json` + `ccts_readiness.json` (covered goods / obligated sectors, industry→coverage map, dated timeline + readiness checklist, each fact cited to EU Commission / ICAP with URLs); `src/lib/regulatory-readiness.ts` (pure `assessCbam`/`assessCcts`/`assessRegulatory`); `ReportOutput` gains `regulatory` (computed in `report-generator.ts` from the form); `src/components/RegulatoryReadiness.tsx` + a `beyond-brsr` entry in `ReportView` `TABS`. **Free / on-device**, consistent with the other report modules; honest framing throughout ("a readiness check, not a liability calculation"). **Verified:** logic probe (steel+EU → both "applies"; textile → CCTS "applies" / CBAM "unlikely"; chemicals → CCTS "may apply"; it_services → both "unlikely"); clean build (the type-only circular import `types.ts`↔`regulatory-readiness.ts` resolves); **full in-browser run (Playwright)** — generated a steel+EU report, opened Beyond BRSR, confirmed both cards render "Likely applies" with the 2026 timeline, checklists, sources + framing (screenshot reviewed). Branch `feat/cbam-ccts-readiness` → master, deployed. **Also committed** the previously-untracked finished docs (`docs/importer-feature|pro-pitch|pro-strategy|webinar-prep.md`), design references + `AGENTS.md` (stale Codex context — refresh later); gitignored `.playwright-mcp/` + `.agents/`. **Next: roadmap Phase 5 — template/knowledge library.**

**Free report: Scope 3 screening calculator on P6-L2 (shipped 2026-06-25):** Roadmap Phase 3. An on-device, **standards-based** Scope 3 GHG screening calculator on the free report's **P6-L2** (Scope 3) row, mirroring the existing Scope 1&2 / energy / water calculators. **Accuracy was the explicit requirement.** Method = **GHG Protocol Corporate Value Chain (Scope 3) Standard** (activity/distance-based); factors = **DEFRA/DESNZ 2024 GHG Conversion Factors v1.1**, cited per-factor (value + exact table/row + URL + vintage) in `src/data/scope3_factors.json` (22 factors); air includes radiative forcing, freight is direct + WTT; values cross-checked against independent sources (e.g. national rail 0.03546, long-haul business 0.58028). **Scope (user decision): activity-based only** — **Cat 6 business travel, Cat 7 commuting, Cat 4/9 transport & distribution, Cat 5 waste**. **Cat 1 purchased goods is DEFERRED** and flagged in-UI ("needs supplier/spend data") — there's no authoritative free India spend-factor set and a foreign-currency EEIO proxy would undercut the accuracy claim. Honestly framed as a **screening estimate** (BRSR Scope 3 is a P6 Leadership/voluntary indicator; Scope 1&2 stay the assured ones). New `src/lib/scope3-calculator.ts` (`Scope3Inputs`, `calcScope3` → per-category subtotals + total, kg→÷1000; `SCOPE3_GROUPS` for the UI) + `src/components/checklist/Scope3Calculator.tsx` (grouped inputs + results + methodology footnote). Wired `scope3Inputs` through `useChecklistState` (state + localStorage persistence), `DataChecklist` → `PrincipleSection` → `DisclosureRow` (renders on `item.id === "P6-L2"`; reuses the shared `turnover_crore` for intensity — no new turnover input). **Verified:** factor cross-check; calc probe (hand calcs match exactly — 1000 pkm long-haul air = 0.26128 t, 10000 t·km rail = 0.347 t, 5 t mixed landfill = 2.60167 t); clean build; **full in-browser run (Playwright)** — generated a report, expanded P6-L2, entered activity data, confirmed all four categories + the Cat 1 deferred note render and the total computes correctly (3.21 tCO₂e) live. **Dev note:** running `next build` then `npm run dev` on this Windows machine corrupts `.next` (a `webpack.js` ENOENT 500 on `/report`) — `rm -rf .next` before `npm run dev` fixes it. Branch `feat/scope3-calculator` → master, deployed. **Next: roadmap Phase 4 — CBAM + CCTS readiness modules (lightweight).**

**Collect: assurance-readiness pack / data-ownership ledger (shipped 2026-06-25):** The market research (`docs/market-research.md`) found the rising 2026 buyer pain is **assurance** — BRSR Core reasonable assurance now asks "show the **evidence trail and named data owners** behind every KPI," not just "is the number right?" Collect already captured all of it (per-input attribution, evidence files, cited factors) but it was scattered + under-sold. This surfaces + exports it as **one artifact**. New `src/lib/datarequest/assurance.ts`: `buildAssuranceLedger(campaign)` → a CSV ledger, one row per **received** data point (BRSR code, disclosure, section/principle, value, unit, prior year, **data owner** + email, **evidence document** + attached Yes/No, and the **cited calculation basis** for computed figures) + a methodology/no-fabrication footnote; `assuranceStats()` → collected/total/withEvidence/owners. `emissions.ts` `EmissionInput` gained `fieldId` (join key so the ledger attaches each computed figure's cited factor to the right owner's row). New `AssurancePackButton.tsx` (on-device CSV via the formula-safe `downloadCsv`). `requests/[id]/page.tsx` got an **"Assurance readiness"** panel (shown when the campaign has items) that reframes the attribution+evidence as the data-ownership trail, with three stat chips + the export button (gated on collected > 0). **Pure logic, no schema/env change, no new emission factors.** Also folded the four research-validated angles (assurance pack, **supplier-lite Collect**, **clone-last-year campaign**, **unit/sanity validation**) into `docs/pro-roadmap.md`. **Verified:** clean build; runtime render on real authenticated campaigns (panel shows on campaigns with items, hidden on empty ones, export gated on collected data, stat chips compute correctly — e.g. "7/7 data points collected · 0 with evidence · 3 data owners"); live prod smoke (`/login` 200, `/requests` 307); **the Pro-website Pro grid was also visually confirmed in-browser (Playwright reconnected)** — one forest Collect hero + two light "Available now" cards (importer, cross-framework export) + three roadmap cards. **Next: roadmap Phase 3 — Scope 3 calculator (careful cited-factor sourcing).** Branch `feat/assurance-pack` → master, deployed.

**Website: shipped Pro features marked live + external market-research doc (shipped 2026-06-25):** The compliance importer (Phase 1) and cross-framework export (Phase 2) had shipped but the landing page still listed them as "Coming." Updated `LandingPage.tsx`: the two pillars are now **"Available now"** and the three live cards (Collect, importer, cross-framework export) lead the grid, with the roadmap cards (proposal builder, multi-client workspace, consultant network) following. `ProPillar` gained a **`flagship`** flag so only Collect keeps the forest-filled hero treatment — the other live cards are light with a green tag (so "three are live" reads as momentum, not three heavy dark blocks). **Honesty fix:** the cross-framework export actually ships **on-device in the free report**, so the Free card now reads "downloadable cross-framework mapping" too, and the Pro pillar drops the overstated CBAM claim ("CBAM and CCTS join as they ship"). The Free-vs-Pro Pro card gained the two now-live bullets; the "on the way" line now names only genuinely-unbuilt items. Also added **`docs/market-research.md`** — external web/Reddit/LinkedIn research on Indian ESG-consultant pains (ranked by evidence strength, with source URLs + quotes), the existing-tool gap landscape (Indian ESG SaaS ₹2L–8L+/yr, pricing-opaque, enterprise-only → the affordable long-tail is unserved), a dated regulatory-driver timeline (BRSR Core glide path, value-chain FY26-27, CBAM definitive period live now, CCTS ~490 entities, Scope 3), and four validated-but-not-yet-built angles: **(1) headline the assurance audit-trail** (we under-sell Collect's attribution+evidence as a data-ownership ledger — the rising 2026 assurance buyer pain), **(2) supplier-lite Collect** for 2%+ value-chain MSME partners, **(3) clone-last-year campaign** as a retention hook, **(4) unit/sanity validation** at data entry. **Honest caveat in the doc:** first-person consultant chatter is sparse online and business/pricing pains are unproven → the webinar (27 Jun) + interviews are how monetization gets de-risked. Corroborates `docs/pro-strategy.md` (the WhatsApp analysis). **Verified:** clean build; prerendered + live HTML confirm 3 "Available now" / 2 "Coming" / 1 "Future" + the new copy. Branch `feat/pro-website-shipped` → master, deployed. **Not browser-eyeballed** (Playwright MCP not connected) — a visual pass of the two-row Pro grid is the recommended manual check.

**Pro roadmap Phase 2: one-click cross-framework export (shipped 2026-06-25):** The report's **Alignment** tab displayed the BRSR↔GRI↔TCFD↔IFRS↔TNFD crosswalk (77 mappings) + the principle-level MSCI/DJSI ratings crosswalk (9) but you couldn't take it out of the tool — and "collect once, report many" (multi-framework translation) was a top pain in the WhatsApp analysis. Added a **contextual "Export (CSV)" button** in the Alignment header that downloads whichever sub-tab is active (the framework mapping, or the ratings alignment). **100% on-device** — reuses the report's RFC-4180/formula-safe `downloadCsv` (`export.ts`), nothing leaves the browser — so it ships in the **free report** and reinforces the multi-framework Pro story. New `src/lib/framework-export.ts` (`buildFrameworkExportRows` — every mapping, all framework columns, em-dash placeholders blanked; `buildRatingsExportRows` — MSCI/DJSI list fields semicolon-joined; pure → `string[][]`); `ReportView.tsx` `AlignmentWorkspace` gained `clientName` + the single export button. **v1 omits a per-row client-status column** on purpose: `framework_mappings` ids (`P1-G1`, `P3-L1`, `P5-H1`, `P6-E1..E27`) are a **different id-space** than the gap-analysis checklist (`P1-E1`, `P6-L2`), so a status join would be mostly blank/misleading — it's a crosswalk *reference* export. TNFD columns populate at runtime (the export uses the merged `report.frameworkMappings`; `report-generator.ts` merges the `tnfd_mappings.json` overlay). **Verified:** clean build; pure-builder probe against real data (77×11 framework rows, 9×7 ratings rows, no injection-guard cells); live prod smoke (`/`, `/report` 200). **Not browser-click-verified** (Playwright MCP not connected) — opening the downloaded CSV is the manual pass. Branch `feat/cross-framework-export` → master, deployed. **Next: roadmap Phase 3 (Scope 3 calculator), then CBAM/CCTS readiness, template library, proposal builder; accounts+billing last.**

**Pro roadmap doc + Phase 1: AI compliance importer (shipped 2026-06-25):** Wrote a full phased Pro build roadmap (`docs/pro-roadmap.md`, 7 phases in build order, each grounded in existing code) covering everything not-yet-built — compliance importer, cross-framework export, Scope 3, CBAM/CCTS readiness modules, template library, proposal/fee builder, and the deferred accounts+billing+multi-client foundation. **Locked decisions:** importer first; keep the shared passcode + manual onboarding for now (auth/Razorpay later); CBAM/CCTS ship as lightweight *readiness* modules first (not liability calculators). Then **built Phase 1 — the AI compliance importer** (the wedge): on a Collect campaign (`/requests/[id]`), the consultant uploads a client's existing report (last year's BRSR / annual report) and the figures are **pre-filled as reviewable suggestions** for the fields they're collecting, instead of hand-copying. The PDF text is extracted **in the browser** (`extractPdfText`, file never leaves the device); only the text is sent to the grounded Groq client (`gpt-oss-20b`, `temperature:0`) with an **extract-only** prompt → returns `{itemId, fieldId, value, source, confidence}` per hit, each carrying the **exact source sentence**. **No fabrication:** the model may only return values literally present; nothing auto-applies — an amber "verify before applying" review surface shows each suggestion (source line + confidence badge + editable value), and only the **ticked** rows write (via the existing `db.updateItem` path → status `received`). New `src/lib/datarequest/importer.ts` (`extractValues` — chunks text ≤6×9k chars with an honest `truncated` flag, merges by fieldId keeping highest confidence, never throws); `actions.ts` `importDocumentAction` (value-returning, `requireConsultant`, returns `configured`/`truncated`/`suggestions`) + `applyImportAction` (writes ticked rows); `src/components/datarequest/ImportPanel.tsx` (collapsible, mounted below Add-owner on the campaign page; hint shown when the campaign has no items yet). **Pro/backend only** (an AI call ⇒ text leaves the browser) — the free tool's on-device keyword detector is untouched. **No schema change** (writes to existing columns; the 6 Groq keys are already on Vercel prod). **Verified:** clean build; a grounded-extraction probe against the **real** model (extracted only the present values verbatim with their source sentences, correctly **omitted an absent field** — no fabrication — and preserved Indian number formats); runtime render of both `ImportPanel` states on real authenticated campaigns; live prod smoke (`/login` 200, `/requests` 307). **Not browser-click-verified** (Playwright MCP not connected) — the upload→review→apply click-through is the recommended manual pass. Branch `feat/compliance-importer` → master, deployed to production. **Next roadmap phases (when ready): Phase 2 cross-framework export (cheapest — data already exists), then Scope 3, CBAM/CCTS readiness, template library, proposal builder; accounts+billing last.**

**Collect: per-client saved-contacts directory + one-tap owner import (shipped 2026-06-24):** In Collect, a campaign **is** one client, and adding a data owner meant typing name+email every time. New **saved-contacts roster per client**: save the client's people once (name, email, role) — by hand, or by **smart-paste** (one per line, split on **tab OR comma**, so a block from the client's email/WhatsApp *and* cells copied straight from Excel/Sheets both parse; the email is required, the rest become name then role) — then **import a data owner with one tap** (a "Pick from saved contacts" row in `AddOwnerPanel` fills name+email; you still pick the fields + send). Every owner you add the normal way is **auto-captured** into the roster too, so it fills itself. Scope = **per-campaign** (not a cross-client/global address book — that wants per-consultant accounts, the deferred monetization phase). New `src/components/datarequest/DirectoryPanel.tsx` (list + add form + collapsible paste box, on the campaign detail page above Add-owner); `db.ts` `listCompanyContacts` / `upsertCompanyContacts` (on-conflict merge by `(request_id,email)`) / `deleteCompanyContact`; `actions.ts` `addDirectoryContactsAction` / `deleteDirectoryContactAction` (both `requireConsultant()`-guarded) + the auto-capture in `addContactAction`; `CompanyContact` type. **Fetched separately + best-effort** (the page does `Promise.all([getCampaign, listCompanyContacts])`; `listCompanyContacts` swallows errors → `[]`), **not** embedded in `getCampaign`'s select — so Collect keeps working **before the migration runs** (directory empty, picker hidden, auto-capture no-ops); it "lights up" once the table exists. **USER TO-DO — run this SQL in the Supabase SQL editor** (PostgREST can't DDL):
```sql
create table if not exists brsr_company_contacts (
  id uuid primary key default gen_random_uuid(),
  request_id uuid not null references brsr_requests(id) on delete cascade,
  name text, email text not null, role text,
  created_at timestamptz not null default now(),
  unique (request_id, email)
);
alter table brsr_company_contacts enable row level security;
```
Build clean. **Not browser-verified this session** (Playwright MCP was disconnected, and full E2E needs the table created first) — verified via build + code review + graceful-degradation design; do a Collect run once the SQL is applied (add a contact + paste a list → appears in the roster; Add owner → one-tap fills name+email → send → owner auto-captured; delete a contact). Branch `feat/collect-contacts-directory` → master.

**"Saved on this device" notice + session back up / restore (shipped 2026-06-24):** The free tool keeps all work in `localStorage`, so a user clearing browser data / using a different browser/device loses it (closing the browser or restarting the laptop is **safe** — localStorage survives that; the loss cases are clearing site data, private mode, or a different browser/device). To keep this honest without breaking the "client data never leaves your browser" promise (and **without** adding accounts/login — the user explicitly weighed Google auth and rejected it as it would reverse the privacy pitch + add friction; accounts belong to the later paid tier, opt-in), we shipped: (1) a **persistent, always-visible notice** in the report sidebar footer ("Saved on this device · your work stays in this browser, a refresh/restart is safe, clearing browser data or switching browsers starts fresh, so back it up or export the PDF/CSV"), replacing the old "No data stored · No login" line; (2) a **"Back up work"** button (report sidebar) that downloads the session as `saaksh-backup-<client>.json`; (3) a **"Restore your work from a backup file"** control on `/start` that re-imports it and goes to `/report`. New `src/lib/storage.ts` helpers `buildSessionBackup()` / `restoreSessionBackup()` (snapshot/validate form + checklist + materiality; a non-Saaksh file is a safe no-op) and new `src/components/SessionBackup.tsx` (`BackupWorkButton` + `RestoreWorkButton`). 100% on-device — the backup file stays with the user, nothing is uploaded; also lets a consultant move work between browsers/devices. Build clean. **Note:** shipped on a clean build + code review only — the live browser smoke test could not run this turn (the Playwright MCP tool disconnected mid-session); a visual pass of the sidebar notice + download/restore round-trip is a recommended follow-up. Branch `feat/session-backup` → master.

**Company identity avatars — deterministic, on-device (shipped 2026-06-24):** The company identity chip (the report's sidebar client card + every row of the company autocomplete) now shows a **polished colored avatar** — a deterministic brand colour derived from the company name + a crisp monogram — instead of the old flat `bg-brand-600` square. New `src/components/CompanyAvatar.tsx` (`name` → hashed colour from a 7-tone deep Evergreen & Ember palette, white initials, configurable `size`); used in `ReportView.tsx` (client card, size 28) and `CompanyAutocomplete.tsx` (dropdown rows, size 24). Same name → same colour, so a company looks consistent across the autocomplete and the workspace. **Why not real logos:** the user asked for actual company logos, but auto-bundling real square logos **on-device** isn't attainable here — Clearbit's logo CDN is dead, logo.dev is token-gated + hotlink-only (a runtime external call that would break the "client data never leaves your browser" promise), Google's favicon endpoint returns an error/generic icon to a scripted fetcher, and Wikimedia only has wide *wordmarks* that don't fit a square chip. So (user's call, from a 3-way choice) we shipped the **on-device polished avatar** — IP-clean, no network call, works for any company including free-text names. (Deferred real-logo paths if ever wanted: an optional per-client logo *upload* stored on-device, or logo.dev with the accepted privacy trade-off.) Verified with Playwright: distinct avatars per company in the autocomplete, and the matching avatar on the workspace card. Build clean. Branch `feat/company-avatar` → master.

**Free tool now uses real URLs — landing / form / report as distinct routes (shipped 2026-06-24):** The free tool used to swap its three views (marketing landing, intake form, report workspace) with React state inside a single `/` route, so the URL never changed — which confused users (the browser Back button left the tool, you couldn't refresh/bookmark into the report, and clicking the brand/logo silently teleported you). Split into three real App Router routes: **`/` = landing** (marketing, still the SEO homepage), **`/start` = intake form**, **`/report` = report workspace**. Navigation is now `useRouter().push`. Still 100% on-device — `/report` regenerates the report locally from the saved intake form (nothing stored server-side), so a refresh or bookmark of `/report` just rebuilds it; with no saved form (e.g. a stale bookmark after "New report") it redirects home. `src/app/page.tsx` is now the slim landing route; new `src/app/start/page.tsx` (the form chrome moved out of the old page.tsx) and `src/app/report/page.tsx` (loads the saved form → `generateReport` → `<ReportView>`, with a one-frame "Preparing your workspace…" state). `ReportView` got an **`onHome`** prop — the sidebar brand now goes to `/` (conventional logo-as-home, with a visible URL change) instead of silently jumping to the form; "Back to form" → `/start` (answers kept), "New report" → clears the session and → `/start`. `storage.ts` dropped the obsolete per-tab `sessionStorage` "active session" flag (the URL is now the source of truth for which view) and gained shared `loadSavedForm()` / `saveForm()` / `clearReportSession()` helpers used by the three routes. The Collect side (`/requests`, `/login`, `/submit/[token]`) already had real routes and is untouched; `middleware.ts` only guards `/requests/*` so `/start` + `/report` are public. Nice side effect: GA4 / Vercel Analytics now see distinct `/`, `/start`, `/report` paths. Verified with Playwright end-to-end: Start → `/start`, submit → `/report`, refresh `/report` stays + regenerates, brand → `/` (resume banner → back to `/report`), Back button steps between routes, "Back to form" pre-fills, "New report" clears + a stale `/report` redirects home. `scripts/e2e-test.js` updated to open the form at `/start` (its label maps are independently stale). Build clean. Branch `feat/real-routes` → master.

**Company-facing PDF redesign + "Pro" badge on the Collect nav (shipped 2026-06-24):** Re-skinned the code-generated "BRSR DATA REQUEST" brief (`src/lib/report-pdf.ts`, jsPDF, client-side, on-device) to the Claude-Design "Evergreen & Ember" mockup (kept at repo root `BRSR Data Request.html` + two screenshots). **Data flow unchanged** — still reads `ReportOutput` (real client, FY, status counts, Section-C gap items grouped by team, plain-English labels from `brsr_plain_language.json`, "already covered" from `already_tracked` items); this was a **visual upgrade**, not new data plumbing. New look: header with the forest "S" logo + serif "Saaksh" + mono "BRSR DATA REQUEST" + right-aligned client/FY/Prepared + a forest rule; a white **"WHERE THINGS STAND"** card with mint/gold/ember **dotted counts** ("N already covered · N to confirm · N to provide" of `summary.totalDataPoints`); team-grouped sections each with a **serif heading + mono team chip** (teal `#E3F7F0`/`#0B6B4F`) + a "Usually found in … forward to …" subtitle + **rounded tick-box rows** (plain label + right-aligned unit, hairline dividers); a **page-2+ "… · CONTINUED" header**; a mint **"Already covered — no action needed"** callout (filled-check, honest line built from the client's actual `selectedFilings`, real covered items single-column wrapped — **no truncation**); a closing "send it back to your Saaksh consultant" note; and a per-page footer "Prepared with Saaksh · cited to SEBI BRSR Format & ICAI (2024) · Page X of Y". **Fonts:** jsPDF built-ins map the design's roles — `times` (serif headings/wordmark), `helvetica` (body), `courier` (mono labels) — same hierarchy, zero bundle weight (embedding the real Newsreader/Hanken/IBM-Plex TTFs is impractical offline on this machine; only woff2 are available). **Grouping stays per-principle and comprehensive** (every gap listed, empty principles skipped) — same scope as the old PDF, so a low-coverage manufacturer can run ~6 pages (that's by design; the brief is the forward-to-teams checklist, the CSV is the formal dump). Reporting FY is now computed (most recently completed Indian FY). **Collect nav now carries a "Pro" badge:** the free report's sidebar "Collect" item (`ReportView.tsx`, a `<Link href="/requests">` visible to free users) gets a small mono **"Pro"** pill (teal chip), so it reads as the paid tier; link unchanged (sign-in still offers "Request Pro access"). Verified end-to-end with Playwright: drove the intake form (steel manufacturer + PCB/HazWaste/PAT filings), generated the report, captured the downloaded PDF and eyeballed all 6 pages (header, dotted summary, chips, rows, continued header, fixed callout, footer), and confirmed the "PRO" badge renders in the sidebar. Build clean. Branch `feat/pdf-redesign` → master.

**Homepage positioning refresh — AI-clear, pain/outcome-led, explicit Free-vs-Pro (shipped 2026-06-24):** Copy + one new section on the Evergreen homepage (`LandingPage.tsx`), no visual rebuild. The user flagged that the redesign read well but the *positioning* was soft on three things. Fixes: (1) **AI is now in the hero.** The hero subtext was rewritten **pain-led → outcome-led**: it opens on the consultant's first-week scramble (a blank 108-field format, scattered filings, no quick read on what's covered) and lands on the outcome (Saaksh turns it into minutes, with a **plain-English AI explanation on every field**, cited + on-device). The trust microline now reads **"Client data never leaves your browser · Plain-English AI · Cited to SEBI & ICAI"** (added the AI signal). (2) **Honest time/outcome line** above the stats band: *"The first week of a BRSR engagement, done in minutes. Gap-analysed, cited, and ready to send."* — qualitative, **no fabricated stat** (matches the no-fabrication ethos; user chose this over a made-up number). (3) **Explicit Free-vs-Pro section** (new, after the Collect section, before Modules) — a two-card compare that **settles the "are the calculators free?" question: they are free.** Free card (light, "No login") = gap-analysed action plan, **built-in GHG/energy/water calculators**, suggested materiality + cross-framework mapping, plain-English AI on every field, CSV + client-ready PDF. Pro card (dark forest, "Paid") = everything in Free **plus** Collect (chase data from the client's team with branded emails + auto-reminders, no-login owner submission + evidence, emissions **auto-computed and attributed**, the **grounded AI narrative draft**), carrying the existing **"Request Pro access"** CTA (`REQUEST_ACCESS_URL`). The dark "AI where it helps, never where it can mislead" section is **kept** (the grounded/never-invents/cited trust framing stays). Also fixed a pre-existing em-dash in the Collect copy and removed an unused `INK` const. Verified with Playwright (desktop + mobile 390px): hero, time/outcome line, and the stacked Free-vs-Pro cards all render; build clean (no warnings). Branch `feat/positioning` → master.

**Launch gating — Collect = "Pro" via manual request-access (shipped 2026-06-24):** Monetization stance for launch (user's call): **no automated billing yet.** Collect stays behind the existing single `CONSULTANT_PASSCODE` gate (`middleware.ts` + `/login`) so free users can't reach it; it's now *presented* as **Pro** with a **"Request Pro access"** CTA on the homepage Collect section (`LandingPage.tsx`) + a link on `/login`, both pointing at `REQUEST_ACCESS_URL` (new `src/lib/links.ts` — a `mailto:rahulu626@gmail.com` with a prefilled subject/body for now; **swap that one constant** for a Calendly / Tally / Google-Form URL when one exists). First customers are onboarded + billed **manually** (UPI/invoice); the shared passcode is acceptable for that handful. No public pricing grid (price discussed 1:1). **Deferred (next pieces):** **automated billing** — Razorpay + per-user Supabase Auth + RLS (replaces the shared passcode) once a few are paying manually. (The **company-facing PDF redesign** is now **shipped** — see the top of the Project Status block.)

**Security hardening (shipped 2026-06-24, from a Gemini code review — verified findings only):** Two real issues fixed. (1) **Server actions now self-authenticate.** Next.js server actions are dispatched by the `Next-Action` header and can be POSTed to *any* route, so the `/requests/*` middleware matcher alone did not protect them — an unauthenticated request could invoke them. `createCampaignAction`, `addContactAction`, `generateNarrativeAction` now call `requireConsultant()` (new `src/lib/datarequest/guard.ts` — `server-only`, checks the `bk_auth` passcode cookie via `next/headers`, `redirect("/login")` on mismatch) as the first line, as defence-in-depth on top of the middleware; `submitDataAction` stays public (token-validated). (2) **Email XSS fixed.** `src/lib/datarequest/email.ts` now HTML-escapes every user-controlled value (client/owner names, contact name, reporting period, field labels) via an `esc()` helper before it enters the HTML body; plain-text subjects are left as-is. Regression test: `src/lib/datarequest/email.test.ts` (3 cases). **Verified already-safe, left unchanged:** the `bk_auth` cookie already sets httpOnly/secure(prod)/sameSite=lax/maxAge (`auth.ts`); `storage.ts` already `sanitize()`s upload filenames and UUID-prefixes the object path (no traversal); PostgREST queries already `encodeURIComponent` user values; secrets are `server-only`; Next redacts server errors in prod. **Deferred (roadmap, not quick fixes):** per-user auth + Supabase RLS (replaces the single shared passcode) and rate-limiting on the public submit/cron endpoints (needs a Vercel KV / Upstash store).

**New homepage + app-wide "Evergreen & Ember" design system (shipped 2026-06-24):** Implemented the Claude-Design homepage and rolled a full new design system across the whole app. **Fonts (self-hosted, app-wide):** Newsreader (display serif), Hanken Grotesk (body), IBM Plex Mono (mono) — downloaded once by `scripts/fetch-fonts.mjs` into `src/app/fonts/` (this machine can't use `next/font/google`); wired via `localFont` in `layout.tsx`, `tailwind.config.ts` (`fontFamily.sans`=Hanken, `.mono`=IBM Plex, `.display`=Newsreader), and `globals.css` (`.font-display` → Newsreader, the `*` rule → Hanken). The old Geist files remain only as unused fallbacks. **Palette (Evergreen & Ember):** `tailwind.config.ts` `forest` → deep evergreen `#0E4A36`/`#0B3A2A`, `brand` scale → mint→green (500 `#18C39A` accent, 700 `#0B6B4F` contrast-safe label-green, 800 `#0E4A36`), plus `ember`/`gold` tokens; `globals.css` vars + body bg `#F7F6F2`→`#FAF8F3` + hairline + glow + badge classes; ~27 hardcoded hexes swapped across 10 files (`#F7F6F2`→`#FAF8F3`, logo chips `#111`→`bg-forest`, the PDF accent + email logo/CTA → evergreen). **Status colours now match the design:** Ready=green, Verify=amber, Collect=**ember/orange** (`checklist/constants.ts` `STATUS_META`, the Overview stats/bar/legend/principle-breakdown in `ReportView.tsx`, and the `.badge-*` classes). The upload-card + "Last year" indigo theme was recoloured to brand. **Homepage:** `src/components/LandingPage.tsx` fully rebuilt as a React component from the design export — header, hero + live readiness panel, sources bar, 3-step how-it-works, action-plan panel with the In-plain-English block, materiality SVG scatter, cross-framework table, a **working** GHG calculator (`useState`), the dark "AI where it helps, never where it can mislead" section, the Collect data-requests panel with the AI-drafted narrative, the modules roadmap, trust pillars + founder note, stats band, dark final CTA, and footer. All panels are live HTML/CSS/SVG (no images). The global `HelpWidget` floating button is restyled to the dark **"Ask Saaksh"** pill from the mockup; `onStart` + the resume banner are preserved. **Verified** with Playwright: the homepage matches the mockup, and the report + Action Plan render on the new palette + fonts with green/amber/ember statuses. The design export is kept at `design-references/Saaksh-Home.html`; the reference sites are in `design-references/`.

**Free-tier AI: pre-generated field explainers (shipped 2026-06-23):** Every BRSR Section-C field in the Action Plan now shows an **"In plain English"** block (top of the expanded row) — a short plain-English explanation of what the disclosure asks for and where a company gets it, tagged **"AI-written"**. **Zero ongoing tokens, no runtime API, fully on-device:** the 108 explanations were generated **once** by `scripts/generate-field-explainers.mjs` (Groq `openai/gpt-oss-20b`, `reasoning_effort: low`, ~27k tokens total across the 6 keys, one time) and shipped as **static data** in `src/data/brsr_field_explainers.json`. The UI (`src/components/checklist/DisclosureRow.tsx` — `EXPLAINERS[item.id]`) just reads the JSON — no fetch, nothing about the client is sent anywhere, so "client data never leaves your browser" stays fully true. Chosen specifically because the user's 6 Groq keys are free-tier + shared across other products, so a live/public AI endpoint (token-burn + abuse risk) was rejected. Verified: 108 generated (zero failures, spot-checked for accuracy), build passes, the block renders on row-expand with **no network call**. Regenerate (resumable) via the script. **Deliberately NOT built:** a live "Ask AI" endpoint (would burn the shared free keys); the keyword help bot + Compliance Chat cover open questions. **Future "can't-stop-using-it" lever flagged (not built):** a multi-client workspace.

**AI narrative draft — Collect/paid tier (shipped 2026-06-23):** The draft page (`/requests/[id]/draft`) now has a **"Generate AI narrative"** button that turns the collected principle-wise (Section C) values into a short **grounded** qualitative paragraph per principle, labeled **"AI draft — review and edit before filing"**, each with a Copy button. **No fabrication:** the model gets only the submitted facts and is instructed never to invent or alter a number (it writes `[to be completed]` for gaps); the deterministic figures still come from `draft.ts`. Uses **Groq** (OpenAI-compatible endpoint `https://api.groq.com/openai/v1/chat/completions`) — model **`openai/gpt-oss-20b`** with `reasoning_effort: "low"` (validated as faithful + token-cheap, ~250 tokens/principle; chosen over llama-3.1-8b which editorialised, and over the deprecated llama-3.3-70b), rotating across the user's **6 `GROQ_API_KEY*` keys** to spread free-tier limits. Files: `src/lib/datarequest/groq.ts` (best-effort fetch client, key rotation, never throws), `src/lib/datarequest/narrative.ts` (`generateNarrative` — groups received Section-C items by principle, one grounded call each), `generateNarrativeAction` in `actions.ts`, `src/components/datarequest/NarrativePanel.tsx` (client UI). Persisted via a new `narrative jsonb` column on `brsr_requests` (`db.setNarrative`, read through the existing `*` select; `Campaign.narrative`). **Generated on demand** (no tokens unless asked) and **best-effort persist** — works even before the column exists. Verified end-to-end against seeded data (grounded, used exact values 1,250 / 82%, no invented numbers, threw `[to be completed]` for gaps). **The 6 GROQ keys are set on Vercel production.** **User to-dos:** (1) run `ALTER TABLE brsr_requests ADD COLUMN narrative jsonb;` in the Supabase SQL editor so narratives persist across reloads (without it, it still generates fresh each time); (2) the keys were pasted in plaintext — rotate them in the Groq console and update `.env.local` + Vercel. (Note: `.env.local` had the keys pasted space-separated from a table; reformatted in place to `KEY=value` so Next loads them.)

**Global help widget + keyword "ask" bot (shipped 2026-06-23):** A persistent floating **Help** button (bottom-right) now appears on **every page** (mounted as `<HelpWidget/>` in `src/app/layout.tsx`; hidden only on `/login`). Its panel has (1) the "How it works" steps and (2) an **ask box** that keyword-matches a curated KB — **no AI, fully on-device**, so it can only surface vetted answers. Files: `src/data/help_topics.json` (~25 entries, `howto` product-usage + `glossary` BRSR-basics), `src/lib/help-search.ts` (pure `searchHelp()` — tokenize + stopword-drop + keyword/title overlap scoring + phrase bonus; `help-search.test.ts`, 8 vitest cases), `src/components/HelpWidget.tsx` (floating button + panel + expandable results + quick-question chips + Compliance-Chat fallback). The report's old top-bar "How it works" button was **removed** (the global widget supersedes it); `WalkthroughModal` was deleted; `Walkthrough.tsx` now exports `StepRow` + `WALKTHROUGH_STEPS` (reused by both the first-run `WalkthroughCard` and the HelpWidget), and the first-run card hint points to the Help button. **Note:** the homepage redesign + color revamp are being done by the user externally (a redesign prompt + palette + reference sites were provided in chat), so this build made **no homepage changes**.

**Rebrand to Saaksh + platform repositioning + first-run help (shipped 2026-06-23):** The product was renamed **BRSR Consultant Kit → Saaksh** across every surface (free-tool + report + Collect headers/sidebars, `/login`, `/submit`, Resend emails, the jsPDF brief footer + header, `layout.tsx` metadata title, two KB-JSON `source` strings; the "BK" monogram → "S"). "BRSR" the regulation is untouched. The **landing page is repositioned** from "a BRSR readiness tool" to "**Saaksh — a compliance platform that starts with BRSR**": the hero subtext signals the platform, and a new **Modules section** (above the final CTA in `LandingPage.tsx`) shows **BRSR = "Available now"** plus **CBAM / CCTS / broader ESG** as desaturated **"Coming soon"** cards. A **first-run help walkthrough** was added so the product is self-explanatory at launch: a dismissible "New here? Here's the sequence" card on the report **Overview** (persisted via `STORAGE_KEYS.walkthroughSeen`, survives "New report") **plus** an always-on **"How it works"** button in the report top bar that reopens the same steps in a modal — both render from `src/components/Walkthrough.tsx` (`WALKTHROUGH_STEPS` + `WalkthroughCard` + `WalkthroughModal`). **(Now shipped — see the AI-narrative entry at the top.)** an **AI narrative draft** feature for the Collect tier (per-principle grounded prose via **Groq**'s OpenAI-compatible API — model `openai/gpt-oss-120b`, endpoint `https://api.groq.com/openai/v1/chat/completions`, rotating across the user's 6 `GROQ_API_KEY*` keys to spread free-tier limits; persisted as a `narrative` JSONB column on `brsr_requests`, a new Groq client mirroring `email.ts`'s best-effort fetch pattern, hard no-fabrication guardrails) — needs the user to set the `GROQ_API_KEY*` env vars + run one SQL line (`ALTER TABLE brsr_requests ADD COLUMN narrative jsonb;`) first; full design in the plan file. (Switched from Anthropic to Groq per the user; note `llama-3.3-70b-versatile` is deprecated on Groq's free tier as of 2026-06-17.) The report-walkthrough modal mentioned below was **superseded the same day by the global HelpWidget** — see the entry above. A **full visual/color revamp is deferred** (user is sourcing color themes; do it as its own pass so work isn't redone twice).

**Free readiness tool (Tab/report side) — shipped & live:** SEBI source links per disclosure · service-sector differentiation (Business Type toggle + `not_applicable` status) · per-principle best practices · MSCI/DJSI ESG Ratings Alignment section · "Suggested Materiality" reframe + shortlist · upload-last-year's-report client-side detection · company-name autocomplete · **localStorage session persistence** · **DataChecklist decomposed** into the `checklist/` module · **embedded GHG Scope 1 & 2 + energy + water calculators** (P6-E1/E7/E3, cited factors) · **Sources & Methodology** reference panel · A & B collected-tracking + last-year detection · **CSV export** ("Export CSV" on the Action Plan = full Section A/B/C with status/gap/how-to-collect/collected flags; **Materiality "Assessment template"** = suggested topics pre-filled with empty scoring/decision columns for the consultant's stakeholder process, honestly framed as a starting format; client-side `src/lib/export.ts`, RFC-4180 + formula-injection guard + UTF-8 BOM, opens in Excel, nothing leaves the browser) · **TNFD (nature) mapping** in the Alignment tab — an indicative BRSR↔TNFD crosswalk over the nature-related disclosures (water/pollution/waste/biodiversity/land), kept in `src/data/tnfd_mappings.json` and merged in `report-generator`; teal pillar badge + filter + stat card alongside GRI/TCFD/IFRS · **"Download PDF"** report export — a **code-generated** (jsPDF, client-side, on-device) **company-facing "BRSR data request" brief**: the gaps grouped by plain theme + the team that usually owns each item (HR/EHS/energy manager/company secretary) with typical sources, plus an "already covered" note when filings are entered. `src/lib/report-pdf.ts` (`downloadReportPdf`); deliberately NOT the 108-field dump (that's the CSV) and NOT browser-print (no Chrome header/footer or extension overlays). Replaced the old `window.print()` + `PrintReport` path (both retired). **Landing page** (`LandingPage.tsx`) is the homepage at `/`; "Start a free report" → the intake form at `/start`; submit → the report at `/report` (each a real route — see the routing entry at the top of Project Status; the report is regenerated on-device from the saved form). Product North Star doc at `docs/PRODUCT.md` gates new features.

**Collect / data-request product (the paid backend tier) — shipped & live** (the big new thing; lives behind a passcode at `/requests`): consultant creates a per-client collection → adds multiple data owners (one-at-a-time via an "+ Add owner" button, each assigned their fields) → one-click **branded request email** (Resend) → owner fills a **no-login form** → **tracking dashboard** + **submission alert email to the consultant** → **auto-reminder cadence** (daily Vercel Cron) → **emissions auto-calc** from submitted activity data, with **per-input attribution** (input → factor + source → who submitted) and a **GHG methodology statement** → **draft BRSR responses** (deterministic, printable, no fabrication) → **evidence/document attachment** (owners upload supporting bills/invoices per field → stored in private Supabase Storage bucket `brsr-evidence` → signed-URL view for the consultant on the detail page → listed in the draft under "Supporting evidence (for assurance)" with a BRSR Core reasonable-assurance note). Stack: **Supabase** (Postgres via PostgREST + service_role + Storage REST API) + **Resend** + **Vercel Cron** + **middleware passcode auth**. Validated by consultant *Priya*, who also offered webinars/partnership (see `[memory]/priya-call-feedback.md`).

**Collect UI polish (shipped 2026-06-16):** `CopyLinkButton` component replaces raw owner URLs (copy + open affordance); chasing visibility — reminder cadence surfaced on each owner card ("Emailed N days ago · X reminders sent"); teaching empty state for campaigns with no owners yet; `bg-forest` primary buttons + `pressable` micro-interactions across all Collect screens; em-dash copy scrubbed throughout.

**Reporting period + Layer 2 (shipped 2026-06-17, from Priya's feedback):** (1) **Reporting period** — every collection now carries a BRSR financial year (FY select on create, defaulting to the most recently completed year), shown on the request email, the owner's submission form, the campaign header and the draft. (2) **Layer 2 — collect against the full BRSR format** — the Add-owner picker now offers the **entire BRSR format (Section A + B + C, 133 fields)** flattened from the readiness KB (`brsr_data_points.json`) via `brsrRequestFields()`, replacing the curated 9-field MVP list. Each request field carries its **BRSR coordinates** (`field_section` / `field_principle` / `field_indicator_type` columns on `brsr_request_items`), rendered as **code chips** on owner cards and used to **group the draft by Section → Principle** so collected values map straight back into the report. The picker is searchable/collapsible (Section A, Section B, P1–P9, with Essential/Leadership tags). The two granular **activity inputs** (`P6-E1-elec`, `P6-E1-diesel`) are preserved under P6 so the GHG auto-calc is untouched. Verified end-to-end (picker, code chips, emissions, draft grouping).

